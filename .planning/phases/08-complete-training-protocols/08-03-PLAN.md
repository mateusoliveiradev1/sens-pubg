---
phase: 08-complete-training-protocols
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/lib/premium-projection.ts
  - src/lib/premium-projection.test.ts
  - src/app/analyze/complete-training-protocol-view-model.ts
  - src/app/analyze/complete-training-protocol-view-model.test.ts
  - src/app/analyze/results-dashboard-view-model.ts
  - src/app/analyze/results-dashboard-view-model.test.ts
  - src/app/analyze/results-dashboard.tsx
  - src/app/analyze/results-dashboard.contract.test.ts
  - src/app/analyze/results-dashboard.module.css
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03, COACH-04, COACH-05, MON-01, MON-02]
user_setup: []
must_haves:
  truths:
    - "D-02, D-03, D-04, D-13: UI uses a dual contract: technical engine facts plus premium summary; Free is useful and polished, Pro unlocks the full ficha and audit."
    - "D-34, D-39, D-40, D-42, D-43: Downgraded protocol UX feels like an unlock path with reason, impact, repair action, next-level unlock, and remaining value."
    - "D-44, D-46, D-51, D-55, D-57, D-58: Preparation appears as a short contextual checklist with safety copy integrated and non-medical."
    - "D-64, D-68, D-69, D-73: Post-analysis UI shows a record-like-this validation checklist, small cards, Free/Pro tests, and copy-safety constraints."
  artifacts:
    - path: "src/app/analyze/complete-training-protocol-view-model.ts"
      provides: "Projection-ready post-analysis view model for protocol ficha, Free/Pro sections, preparation, validation, blockers, and audit disclosure."
      contains: "buildCompleteTrainingProtocolViewModel"
    - path: "src/lib/premium-projection.ts"
      provides: "Free/Pro projection that summarizes complete protocols without hiding clip truth."
      contains: "projectCompleteTrainingProtocolForAccess"
    - path: "src/app/analyze/results-dashboard.tsx"
      provides: "Premium post-analysis protocol ficha integrated into the result report."
      contains: "completeTrainingProtocol"
  key_links:
    - from: "src/lib/premium-projection.ts"
      to: "src/app/analyze/complete-training-protocol-view-model.ts"
      via: "server-derived Free/Pro truth feeds UI model; UI does not decide entitlements itself"
      pattern: "completeProtocol"
    - from: "src/app/analyze/results-dashboard-view-model.ts"
      to: "src/app/analyze/results-dashboard.tsx"
      via: "view model controls visible protocol rows and audit disclosure"
      pattern: "protocolFicha"
---

<objective>
Build the Free/Pro complete protocol projection and polished post-analysis protocol ficha.

Purpose: make the first result screen answer what to do now while preserving confidence, coverage, blockers, Free value, Pro completeness, and audit disclosure.
Output: server-safe projection, view-model contract, result dashboard UI, CSS, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-training-protocols/08-CONTEXT.md
@.planning/phases/08-complete-training-protocols/08-RESEARCH.md
@.planning/phases/08-complete-training-protocols/08-01-SUMMARY.md
@src/lib/premium-projection.ts
@src/app/analyze/results-dashboard-view-model.ts
@src/app/analyze/results-dashboard.tsx
@src/app/analyze/results-dashboard.module.css
@src/ui/components/pro-lock-preview.tsx
@src/ui/components/evidence-chip.tsx
@src/ui/components/premium-ui.module.css
</context>

<threat_model>
Scope: Free/Pro projection, post-analysis protocol UI, copy, blockers, and audit disclosure.

Threats:
- HIGH: Free looks broken or hides truth evidence. Mitigation: Free projection keeps focus, duration, essential steps, compact preparation, basic validation, confidence, coverage, blockers, and repair CTA.
- HIGH: Client-side UI grants Pro details from local state. Mitigation: projection is created in `premium-projection.ts` and UI consumes projected result only.
- HIGH: UI shows polished but unsupported protocol facts. Mitigation: view model displays limited/estimated/blocked states and audit disclosure from complete protocol fields.
- MEDIUM: protocol card becomes a giant form or nested-card maze. Mitigation: one dominant CTA, 3-5 item sections, disclosures for technical details, no nested cards.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make premium projection protocol-aware</name>
  <files>src/lib/premium-projection.ts, src/lib/premium-projection.test.ts</files>
  <read_first>src/lib/premium-projection.ts, src/lib/premium-projection.test.ts, src/types/engine.ts, src/types/monetization.ts</read_first>
  <action>
Add `projectCompleteTrainingProtocolForAccess(protocol, projection)` to `src/lib/premium-projection.ts`.

Behavior:
- If Pro has `coach.full_plan` and `training.next_block_protocol`, return protocol unchanged.
- For Free, return a summarized protocol preserving:
  - `version`, `id`, `tier`, `title`, `summary`, `environment`, `objective`;
  - `dose.durationMinutes`;
  - first 3 `executionSteps`;
  - first 3 `preparation.items`;
  - first validation target/checklist item;
  - all downgrade reason codes and repair CTAs;
  - confidence/coverage/audit summary strings.
- For Free, remove or shorten:
  - exact reps beyond duration;
  - full stop/continue criteria;
  - full proSections;
  - revision/audit details that belong to Pro continuity.

Update `projectAnalysisForAccess` to project `result.coachPlan.completeProtocol`.

Update lock copy for `training.next_block_protocol` to say: Free keeps foco, duracao, passos essenciais, preparo compacto, validacao basica, confianca/cobertura/bloqueios; Pro adds reps, local, alvo, criterios, preparacao completa, auditoria, revisao, validacao compativel, transferencia real.
  </action>
  <verify>npx vitest run src/lib/premium-projection.test.ts</verify>
  <acceptance_criteria>
    - Free projection test expects `coachPlan.completeProtocol.dose.durationMinutes` to remain visible.
    - Free projection test expects exact reps or full pro sections to be absent/reduced.
    - Pro projection test expects complete protocol object to remain unchanged.
    - Lock body for `training.next_block_protocol` contains `Pro adiciona reps`.
  </acceptance_criteria>
  <done>Free/Pro protocol depth is decided by server-owned projection, not UI guesswork.</done>
</task>

<task type="auto">
  <name>Task 2: Add complete protocol view model</name>
  <files>src/app/analyze/complete-training-protocol-view-model.ts, src/app/analyze/complete-training-protocol-view-model.test.ts, src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard-view-model.test.ts</files>
  <read_first>src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard-view-model.test.ts, src/types/engine.ts, src/types/monetization.ts</read_first>
  <action>
Create `src/app/analyze/complete-training-protocol-view-model.ts`.

Export:
- `buildCompleteTrainingProtocolViewModel(result: AnalysisResult)` returning `null` if no protocol.
- `CompleteTrainingProtocolViewModel` with:
  - `headline`, `tierLabel`, `durationLabel`, `environmentLabel`, `primaryCtaLabel`;
  - `summaryRows` exactly for weapon, optic, distance/range, focus, and target;
  - `essentialSteps` max 3;
  - `preparationItems` max 5 with label/reason/safety;
  - `blockerPanel` with reason, impact, repairAction, unlocksNext;
  - `validationCard` with record-like-this checklist max 8;
  - `transferCard` with situation checklist max 5 and `technicalProofCopy` saying transfer does not replace compatible clip validation;
  - `auditDisclosure` with drillId, version, confidence, coverage, downgrade codes, and limited personalization reasons.

Update `ResultVerdictModel` or surrounding result view model to include `completeTrainingProtocol`.

All formatting is pt-BR and must avoid perfect/guaranteed/rank/medical claims.
  </action>
  <verify>npx vitest run src/app/analyze/complete-training-protocol-view-model.test.ts src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - View-model test for usable analysis returns non-null `completeTrainingProtocol`.
    - View-model test asserts `preparationItems.length <= 5`.
    - View-model test asserts transfer card contains copy equivalent to `nao substitui a validacao compativel`.
    - View-model test asserts blocker panel exists for `missing_distance`.
  </acceptance_criteria>
  <done>Result UI has a compact, deterministic protocol model before JSX changes.</done>
</task>

<task type="auto">
  <name>Task 3: Render the post-analysis protocol ficha</name>
  <files>src/app/analyze/results-dashboard.tsx, src/app/analyze/results-dashboard.contract.test.ts, src/app/analyze/results-dashboard.module.css</files>
  <read_first>src/app/analyze/results-dashboard.tsx, src/app/analyze/results-dashboard.contract.test.ts, src/app/analyze/results-dashboard.module.css, src/app/analyze/complete-training-protocol-view-model.ts, src/ui/components/pro-lock-preview.tsx, src/ui/components/evidence-chip.tsx</read_first>
  <action>
Add a `CompleteTrainingProtocolPanel` inside `results-dashboard.tsx` near the existing coach/next-block result area.

Layout rules:
- First fold shows headline, tier badge, duration, environment, summary rows, and one dominant CTA.
- Show sections: `O que treinar agora`, `Preparar antes do spray`, `Grave o proximo clip assim`, `Transferir para TDM/partida`, and `Auditoria tecnica`.
- Each visible section has 3-5 items except the validation checklist, which may have up to 8 compact items.
- Render blockers as repair path: reason, impact, repair action, and what unlocks next.
- Render Pro locks through existing `ProLockPreview` when projection hides full protocol details.
- Technical audit starts collapsed or visually secondary but remains available.
- Do not build a session runner, timers, or multi-step workflow automation in this plan.

Add/extend CSS classes with stable responsive dimensions and no nested cards.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard.contract.test.ts src/app/analyze/complete-training-protocol-view-model.test.ts src/app/copy-claims.contract.test.ts</verify>
  <acceptance_criteria>
    - Contract test source contains `CompleteTrainingProtocolPanel`.
    - Contract test asserts labels `O que treinar agora`, `Preparar antes do spray`, and `Grave o proximo clip assim`.
    - Contract test asserts no `perfeito`, `garantido`, `rank garantido`, `diagnostico medico`, or `continue com dor` copy appears.
    - CSS contains `.completeProtocolPanel` and `.protocolFichaGrid`.
  </acceptance_criteria>
  <done>Post-analysis result gives Free and Pro users a polished, honest protocol ficha.</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/lib/premium-projection.test.ts src/app/analyze/complete-training-protocol-view-model.test.ts src/app/analyze/results-dashboard-view-model.test.ts src/app/analyze/results-dashboard.contract.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run typecheck
</verification>

<success_criteria>

- Free protocol output remains useful and honest.
- Pro protocol output includes the complete ficha depth.
- Result UI connects protocol to diagnosis, evidence, blockers, preparation, and next compatible clip.
- UI copy remains confidence-bound and avoids perfect, guaranteed, rank, medical, or PUBG/KRAFTON affiliation claims.
- No Phase 9 session runner is built.

</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-training-protocols/08-03-SUMMARY.md` with Free/Pro projection evidence, UI inventory, copy-safety evidence, and focused test results.
</output>
