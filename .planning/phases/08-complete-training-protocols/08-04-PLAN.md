---
phase: 08-complete-training-protocols
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/actions/dashboard-active-coach-loop.ts
  - src/actions/dashboard-active-coach-loop.test.ts
  - src/actions/dashboard.ts
  - src/actions/dashboard.test.ts
  - src/app/dashboard/page.tsx
  - src/app/dashboard/page.contract.test.ts
  - src/app/history/page.tsx
  - src/app/history/page.contract.test.ts
  - src/app/history/[id]/page.tsx
  - src/app/history/[id]/coach-protocol-outcome-panel.tsx
  - src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts
  - src/app/history/history-protocol-view-model.ts
  - src/app/history/history-protocol-view-model.test.ts
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03, COACH-04, COACH-05, MON-02]
user_setup: []
must_haves:
  truths:
    - "D-03, D-08, D-12, D-13: History/detail surfaces expose audit and revision before/after without burying the primary action."
    - "D-28, D-29, D-60, D-61, D-62, D-63, D-64, D-65, D-66, D-67, D-68: Dashboard/history support outcome -> compatible clip -> real-match transfer -> progress/conflict with conservative evidence hierarchy."
    - "D-31, D-37, D-41, D-49, D-50, D-54: Fatigue, pain, confusion, and variable changes downgrade or repair the loop without counting as aim failure."
    - "D-69, D-74: UI/action flow coverage must prove protocol, outcome, compatible validation, transfer, and conflict states."
  artifacts:
    - path: "src/actions/dashboard-active-coach-loop.ts"
      provides: "Active protocol state, next compatible validation checklist, downgrade/repair status, and transfer prompt for dashboard."
      contains: "completeProtocol"
    - path: "src/app/history/history-protocol-view-model.ts"
      provides: "History/detail audit model for protocol snapshots, revisions, outcomes, validation checklist, and transfer records."
      contains: "buildHistoryProtocolViewModel"
    - path: "src/app/history/[id]/coach-protocol-outcome-panel.tsx"
      provides: "Small-card outcome, compatible validation, transfer, and revision UI."
      contains: "fatigue_or_pain"
  key_links:
    - from: "src/actions/history.ts"
      to: "src/app/history/history-protocol-view-model.ts"
      via: "stored fullResult protocol, outcomes, revisions, and transfer records become audit UI"
      pattern: "completeProtocol"
    - from: "src/actions/dashboard-active-coach-loop.ts"
      to: "src/app/dashboard/page.tsx"
      via: "dashboard shows active next action without becoming a session runner"
      pattern: "validation_needed"
---

<objective>
Extend dashboard and history into the complete protocol continuity and audit loop.

Purpose: after the post-analysis ficha exists, make dashboard show the current protocol action and history/detail show outcomes, compatible validation, revisions, transfer, conflicts, and audit trails.
Output: action/view models, dashboard cards, history/detail cards, outcome panel extensions, and contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-training-protocols/08-CONTEXT.md
@.planning/phases/08-complete-training-protocols/08-RESEARCH.md
@.planning/phases/08-complete-training-protocols/08-02-SUMMARY.md
@.planning/phases/08-complete-training-protocols/08-03-SUMMARY.md
@src/actions/dashboard-active-coach-loop.ts
@src/actions/dashboard.ts
@src/actions/history.ts
@src/app/dashboard/page.tsx
@src/app/history/page.tsx
@src/app/history/[id]/page.tsx
@src/app/history/[id]/coach-protocol-outcome-panel.tsx
</context>

<threat_model>
Scope: dashboard active state, history audit/detail state, outcome UI, revision UI, validation prompts, and transfer cards.

Threats:
- HIGH: dashboard implies transfer/self-report is validated technical progress. Mitigation: labels distinguish outcome, compatible clip, and real-match transfer; strong progress requires compatible clip convergence.
- HIGH: outcome panel encourages continuing through pain/fatigue. Mitigation: `fatigue_or_pain` path shows stop/reduce/repair guidance and professional-help copy if symptoms persist.
- MEDIUM: history page hides revision/audit reasons. Mitigation: detail view model exposes previous/revised protocol, changed fields, tier direction, and evidence payload.
- MEDIUM: surfaces accidentally build Phase 9 runner. Mitigation: only cards/actions/prompts; no timers, session state machine, benchmark runner, or guided sessions.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add active protocol state to dashboard actions</name>
  <files>src/actions/dashboard-active-coach-loop.ts, src/actions/dashboard-active-coach-loop.test.ts, src/actions/dashboard.ts, src/actions/dashboard.test.ts</files>
  <read_first>src/actions/dashboard-active-coach-loop.ts, src/actions/dashboard.ts, src/actions/history.ts, src/types/engine.ts, src/app/analyze/complete-training-protocol-view-model.ts</read_first>
  <action>
Extend active coach loop payload with `completeProtocol` fields:
- `protocolTitle`
- `protocolTier`
- `durationLabel`
- `environmentLabel`
- `primaryFocusTitle`
- `nextCompatibleClipChecklist` from protocol validation
- `repairActionLabel` when downgrade exists
- `transferPromptLabel`
- `safetyStopLabel` when preparation contains pain/fatigue stop rule
- `evidenceHierarchyLabel` exactly equivalent to `Resultado registra execucao; clip compativel valida tecnica; partida/TDM valida transferencia pratica.`

Update `getDashboardData` or the active loop builder so Pro users with `coach.validation_loop` see the active protocol state; Free users still see a compact next action and lock copy from projection.
  </action>
  <verify>npx vitest run src/actions/dashboard-active-coach-loop.test.ts src/actions/dashboard.test.ts</verify>
  <acceptance_criteria>
    - Dashboard active loop tests assert `nextCompatibleClipChecklist` includes weapon, optic, distance, and success criterion.
    - Dashboard tests assert Free payload does not expose full Pro protocol audit.
    - Dashboard tests assert evidence hierarchy copy distinguishes outcome, compatible clip, and transfer.
    - No dashboard test expects a timer/session runner field.
  </acceptance_criteria>
  <done>Dashboard can show the active Phase 8 protocol action without overclaiming evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Build history protocol audit view model</name>
  <files>src/app/history/history-protocol-view-model.ts, src/app/history/history-protocol-view-model.test.ts, src/app/history/page.contract.test.ts, src/app/history/[id]/page.tsx</files>
  <read_first>src/app/history/page.tsx, src/app/history/[id]/page.tsx, src/actions/history.ts, src/types/engine.ts, src/db/schema.ts</read_first>
  <action>
Create `src/app/history/history-protocol-view-model.ts`.

Export `buildHistoryProtocolViewModel(input)` with:
- `snapshotCard`: protocol title, version, tier, drill ID, savedAt, focus, duration.
- `outcomeCard`: latest outcome status, evidence strength, pending/needs validation, conflict copy.
- `validationCard`: exact next compatible clip checklist.
- `transferCard`: latest real-match/TDM transfer record and `countsAsTechnicalValidation` copy.
- `revisionTimeline`: rows with revision reason, tier direction, changed fields, createdAt.
- `auditRows`: downgrade codes, blocker reasons, support limitations, confidence/coverage, hidden/visible Free/Pro state.

Update history/detail page to render compact protocol audit sections from the view model. Keep dashboard/history IA from Phase 7; use existing premium primitives and no nested cards.
  </action>
  <verify>npx vitest run src/app/history/history-protocol-view-model.test.ts src/app/history/page.contract.test.ts</verify>
  <acceptance_criteria>
    - View-model tests assert revision row contains `tierDirection`.
    - View-model tests assert transfer copy says it does not count as technical validation.
    - History contract test asserts `Protocolo salvo`, `Validacao compativel`, and `Transferencia em partida/TDM` labels are present.
    - History detail source contains `buildHistoryProtocolViewModel`.
  </acceptance_criteria>
  <done>History can audit the protocol lifecycle and revision trail.</done>
</task>

<task type="auto">
  <name>Task 3: Extend outcome panel for Phase 8 cards</name>
  <files>src/app/history/[id]/coach-protocol-outcome-panel.tsx, src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts, src/app/dashboard/page.tsx, src/app/dashboard/page.contract.test.ts, src/app/history/page.tsx, src/app/history/page.contract.test.ts</files>
  <read_first>src/app/history/[id]/coach-protocol-outcome-panel.tsx, src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts, src/actions/history.ts, src/actions/dashboard-active-coach-loop.ts, src/app/dashboard/page.tsx</read_first>
  <action>
Update `coach-protocol-outcome-panel.tsx`:
- Add outcome choices for `fatigue_or_pain`, `confused`, and `variable_changed`.
- Show three small cards: `Resultado do bloco`, `Grave o proximo clip assim`, `Transferencia em partida/TDM`.
- `fatigue_or_pain` choice must display stop/reduce guidance and copy equivalent to `dor, formigamento ou dormencia interrompe o bloco`.
- `variable_changed` choice must explain strong validation is invalidated when sensitivity, grip, muzzle, optic, distance, stance, weapon, or preparation changed.
- Add action call path for `recordTrainingProtocolTransfer`.

Update dashboard/history pages to show the active protocol CTA:
- dashboard CTA: `Continuar protocolo` or `Gravar validacao compativel`;
- history detail CTA: `Registrar resultado`, `Registrar transferencia`, or `Revisar protocolo` depending on state.
  </action>
  <verify>npx vitest run src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts src/app/dashboard/page.contract.test.ts src/app/history/page.contract.test.ts src/app/copy-claims.contract.test.ts</verify>
  <acceptance_criteria>
    - Outcome panel contract test asserts `fatigue_or_pain`, `confused`, and `variable_changed` appear in source.
    - Outcome panel contract test rejects copy equivalent to `continue com dor`.
    - Dashboard contract test asserts `Gravar validacao compativel`.
    - History contract test asserts `Registrar transferencia`.
  </acceptance_criteria>
  <done>Users can close, repair, validate, or transfer a protocol through small honest cards.</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/actions/dashboard-active-coach-loop.test.ts src/actions/dashboard.test.ts src/app/history/history-protocol-view-model.test.ts src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts src/app/dashboard/page.contract.test.ts src/app/history/page.contract.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run typecheck
</verification>

<success_criteria>

- Dashboard shows the active complete protocol state and next compatible validation action.
- History/detail surfaces show protocol snapshot, outcome, revision, validation, transfer, conflict, and audit information.
- Outcome UI handles fatigue/pain, confusion, and variable change safely.
- Real-match/TDM transfer is visible but cannot replace compatible clip validation.
- No session runner or Spray Lab benchmark is implemented in Phase 8.

</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-training-protocols/08-04-SUMMARY.md` with dashboard/history/action evidence and focused test results.
</output>
