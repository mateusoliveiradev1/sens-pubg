---
phase: 08-complete-training-protocols
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - src/core/coach-llm-contract.ts
  - src/core/coach-llm-adapter.ts
  - src/core/coach-llm-adapter.test.ts
  - src/core/coach-llm-contract.test.ts
  - src/core/coach-golden-runner.test.ts
  - scripts/run-coach-goldens.ts
  - scripts/run-benchmark.ts
  - src/core/benchmark-runner.test.ts
  - tests/goldens/coach/complete-protocol-vertical-control.json
  - tests/goldens/coach/complete-protocol-low-confidence.json
  - tests/goldens/coach/complete-protocol-outcome-conflict.json
  - tests/goldens/coach/complete-protocol-fatigue.json
  - tests/goldens/benchmark/synthetic-benchmark.v1.json
  - tests/goldens/benchmark/synthetic-benchmark.baseline.json
  - src/app/copy-claims.contract.test.ts
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03, COACH-04, COACH-05, BENCH-01, BENCH-03]
user_setup: []
must_haves:
  truths:
    - "D-05, D-09, D-33, D-34, D-35, D-36, D-37, D-38, D-39, D-40, D-41, D-42, D-43: Goldens and benchmark checks must lock drill ID, tier, dose, blockers, downgrade CTAs, and weak-evidence behavior."
    - "D-44, D-45, D-47, D-48, D-49, D-50, D-51, D-52, D-53, D-54, D-55, D-56, D-57, D-58: Copy tests must block medical, strength-program, pain-as-failure, and continue-through-pain language."
    - "D-59, D-60, D-61, D-62, D-63, D-66, D-67: Outcome/history/validation influence must stay conservative and evidence-bound."
    - "D-71, D-72, D-73, D-74: Golden tests cover selection, weak downgrade matrix, Free/Pro projection, copy safety, LLM immutability, and flow contracts."
  artifacts:
    - path: "src/core/coach-llm-contract.ts"
      provides: "Immutable facts include complete protocol technical fields so optional LLM rewrite cannot alter truth."
      contains: "completeProtocol"
    - path: "tests/goldens/coach/complete-protocol-low-confidence.json"
      provides: "Golden downgrade fixture for weak evidence protocol behavior."
      contains: "low_confidence"
    - path: "scripts/run-coach-goldens.ts"
      provides: "Coach golden runner checks complete protocol tier, drill, duration, reps, blockers, and validation."
      contains: "completeProtocol"
  key_links:
    - from: "src/core/coach-llm-contract.ts"
      to: "src/core/coach-llm-adapter.ts"
      via: "LLM output can rewrite only allowed display copy while preserving complete protocol facts"
      pattern: "immutableFacts"
    - from: "tests/goldens/coach"
      to: "scripts/run-coach-goldens.ts"
      via: "golden expectations enforce complete protocol truth"
      pattern: "completeProtocol"
---

<objective>
Harden LLM guardrails, copy safety, coach goldens, and benchmark expectations for complete training protocols.

Purpose: prevent Phase 8 from shipping protocols that are pretty but mutable, unsupported, overconfident, medically unsafe, or untested.
Output: LLM immutable facts, adapter preservation tests, coach golden fixtures, benchmark checks, downgrade matrix, and copy-safety tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-training-protocols/08-CONTEXT.md
@.planning/phases/08-complete-training-protocols/08-RESEARCH.md
@.planning/phases/08-complete-training-protocols/08-01-SUMMARY.md
@.planning/phases/08-complete-training-protocols/08-02-SUMMARY.md
@.planning/phases/08-complete-training-protocols/08-03-SUMMARY.md
@.planning/phases/08-complete-training-protocols/08-04-SUMMARY.md
@src/core/coach-llm-contract.ts
@src/core/coach-llm-adapter.ts
@scripts/run-coach-goldens.ts
@scripts/run-benchmark.ts
@tests/goldens/coach
@tests/goldens/benchmark/synthetic-benchmark.v1.json
@src/app/copy-claims.contract.test.ts
</context>

<threat_model>
Scope: optional LLM rewrite, copy safety, golden/benchmark truth, and downgrade coverage.

Threats:
- HIGH: LLM alters protocol tier, drill, reps, duration, weapon, optic, distance, blockers, validation, or safety facts. Mitigation: immutable facts include complete protocol fields and adapter rejects changed facts/counts/order.
- HIGH: benchmark ignores complete protocol regressions. Mitigation: coach golden runner and benchmark truth include complete protocol expectations.
- HIGH: copy safety misses medical/guarantee claims. Mitigation: copy tests reject perfect/rank/guarantee, treatment/diagnosis, musculacao prescriptions, pain as failure, and continue-through-pain copy.
- MEDIUM: downgrade matrix covers only one weak case. Mitigation: fixtures cover low confidence, missing distance, unsupported/limited support, outcome conflict, variable changed, fatigue, invalid clip, and partial safe read.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend LLM contract immutable facts</name>
  <files>src/core/coach-llm-contract.ts, src/core/coach-llm-contract.test.ts, src/core/coach-llm-adapter.ts, src/core/coach-llm-adapter.test.ts</files>
  <read_first>src/core/coach-llm-contract.ts, src/core/coach-llm-adapter.ts, src/core/coach-llm-adapter.test.ts, src/types/engine.ts, src/core/training-protocols.ts</read_first>
  <action>
Update LLM contract:
- Add complete protocol input to immutable facts when `coachPlan.completeProtocol` exists.
- Immutable facts must include: `version`, `id`, `drillId`, `tier`, primary focus area/title, secondary focus order, environment type, weaponId, opticId, distanceMeters or distanceMode, attachments, sensitivityProfile, durationMinutes, sprayReps, rest seconds, target, validation minimums, stop conditions, continue criteria, downgrade reason codes, repair CTA IDs/copy keys, preparation safety IDs, transfer `countsAsTechnicalValidation`.
- LLM output schema may rewrite only allowed display strings:
  - sessionSummary;
  - primaryFocusWhyNow;
  - actionProtocols[].instruction;
  - nextBlockTitle;
  - optional completeProtocol display strings such as title/summary/executionSteps/preparation labels, but only if counts and IDs match.

Update adapter preservation checks:
- reject changed complete protocol IDs, drill IDs, tier, dose, context, validation, transfer, blocker, preparation safety IDs, or array counts;
- reject blocked copy terms from complete protocol display strings.
  </action>
  <verify>npx vitest run src/core/coach-llm-contract.test.ts src/core/coach-llm-adapter.test.ts</verify>
  <acceptance_criteria>
    - LLM contract test asserts immutable facts include `completeProtocol.drillId`.
    - Adapter test rejects output that changes `durationMinutes`.
    - Adapter test rejects output that changes validation target or transfer technical-validation flag.
    - Adapter test accepts a safe rewrite that changes only protocol title/summary text while preserving IDs/counts.
  </acceptance_criteria>
  <done>Optional LLM can polish protocol copy but cannot create or mutate protocol truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add coach golden protocol matrix</name>
  <files>scripts/run-coach-goldens.ts, src/core/coach-golden-runner.test.ts, tests/goldens/coach/complete-protocol-vertical-control.json, tests/goldens/coach/complete-protocol-low-confidence.json, tests/goldens/coach/complete-protocol-outcome-conflict.json, tests/goldens/coach/complete-protocol-fatigue.json</files>
  <read_first>scripts/run-coach-goldens.ts, src/core/coach-golden-runner.test.ts, tests/goldens/coach/underpull-standard-411.json, tests/goldens/coach/low-confidence-underpull.json, src/core/training-protocols.ts</read_first>
  <action>
Extend coach golden expected truth with `completeProtocol`.

Required expected fields:
- `version`
- `tier`
- `drillId`
- `durationMinutes`
- `sprayReps`
- `environment`
- `target`
- `preparationContains`
- `downgradeReasons`
- `repairActions`
- `validationTarget`
- `transferCountsAsTechnicalValidation`

Add fixtures:
- `complete-protocol-vertical-control.json`: usable vertical focus, Training Mode, vertical drill, 12 or 18 minutes depending tier.
- `complete-protocol-low-confidence.json`: weak evidence downgrade with `low_confidence` and no apply criteria.
- `complete-protocol-outcome-conflict.json`: conflict blocks apply and asks compatible validation.
- `complete-protocol-fatigue.json`: fatigue/pain reduces dose and blocks technical failure copy.

Update `scripts/run-coach-goldens.ts` and tests so missing or mismatched complete protocol fields fail.
  </action>
  <verify>npx vitest run src/core/coach-golden-runner.test.ts && node --version</verify>
  <acceptance_criteria>
    - Golden runner test fails when `completeProtocol.drillId` is removed from a fixture expectation.
    - All four new JSON fixtures contain `completeProtocol`.
    - Fatigue fixture expects `fatigue_or_pain` and copy that stops/reduces the block.
    - Low-confidence fixture expects `apply_protocol` absent from tier.
  </acceptance_criteria>
  <done>Coach goldens lock complete protocol behavior for normal and downgraded cases.</done>
</task>

<task type="auto">
  <name>Task 3: Extend benchmark and copy safety gates</name>
  <files>scripts/run-benchmark.ts, src/core/benchmark-runner.test.ts, tests/goldens/benchmark/synthetic-benchmark.v1.json, tests/goldens/benchmark/synthetic-benchmark.baseline.json, src/app/copy-claims.contract.test.ts</files>
  <read_first>scripts/run-benchmark.ts, src/core/benchmark-runner.test.ts, tests/goldens/benchmark/synthetic-benchmark.v1.json, tests/goldens/benchmark/synthetic-benchmark.baseline.json, src/app/copy-claims.contract.test.ts</read_first>
  <action>
Extend benchmark expected truth `nextBlock` checks with complete protocol checks:
- tier;
- drillId;
- durationMinutes;
- sprayReps range;
- environment type;
- downgrade reason codes;
- validation target;
- transfer `countsAsTechnicalValidation === false`.

Update synthetic benchmark fixtures/baseline so current expected outputs include complete protocol truth.

Extend `src/app/copy-claims.contract.test.ts` blocked phrases/regexes:
- existing perfect/guaranteed/rank/PUBG affiliation terms stay blocked;
- add medical terms: `diagnostico medico`, `tratamento`, `cura`, `lesao diagnosticada`, `continue com dor`;
- add strength prescription terms in product copy: `3 series`, `carga`, `progressao de musculacao`, `treino de antebraco pesado`.

Allow safe short copy such as `pare se sentir dor, dormencia ou formigamento` and `procure orientacao profissional se persistir`.
  </action>
  <verify>npx vitest run src/core/benchmark-runner.test.ts src/app/copy-claims.contract.test.ts && npm run benchmark:gate</verify>
  <acceptance_criteria>
    - Benchmark test asserts `completeProtocol.drillId` is checked.
    - Benchmark fixture contains `completeProtocol` under expected truth for all coach-covered clips.
    - Copy claims test rejects `continue com dor`.
    - Copy claims test allows `pare se sentir dor, dormencia ou formigamento`.
  </acceptance_criteria>
  <done>Benchmark and copy gates protect complete protocols from regression and unsafe claims.</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/core/coach-llm-contract.test.ts src/core/coach-llm-adapter.test.ts src/core/coach-golden-runner.test.ts src/core/benchmark-runner.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run benchmark:gate
- [ ] npm run typecheck
</verification>

<success_criteria>

- LLM rewrite cannot alter complete protocol technical facts.
- Coach goldens cover normal, weak-evidence, conflict, and fatigue cases.
- Benchmark gate checks complete protocol truth.
- Copy safety blocks perfect/rank/guarantee, medical diagnosis/treatment, pain-as-failure, and strength-program prescriptions.
- Safe preparation copy remains allowed.

</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-training-protocols/08-05-SUMMARY.md` with LLM immutable-fact evidence, golden matrix, benchmark changes, and copy-safety results.
</output>
