---
phase: 08-complete-training-protocols
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/types/engine.ts
  - src/db/schema.ts
  - drizzle/0011_complete_training_protocols.sql
  - drizzle/meta/_journal.json
  - src/db/schema.test.ts
  - src/core/coach-outcomes.ts
  - src/core/coach-outcomes.test.ts
  - src/actions/history.ts
  - src/actions/history.test.ts
  - src/app/history/analysis-result-hydration.ts
  - src/app/history/analysis-result-hydration.test.ts
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03, COACH-04, COACH-05]
user_setup:
  - "A Drizzle push/migration may require local DATABASE_URL setup already used by prior phases."
must_haves:
  truths:
    - "D-07, D-08, D-12: Store a versioned protocol snapshot per analysis and support explicit revisions with before/after, reason, new evidence, changed fields, and tier direction."
    - "D-31, D-36, D-37, D-41, D-49, D-50, D-54, D-56: Fatigue, pain, confusion, invalid capture, and variable changes are execution/safety signals, not automatic technical failure."
    - "D-59, D-60, D-61, D-62, D-63, D-64, D-65, D-66, D-67, D-68: Outcome, compatible clip, and real-match/TDM transfer form a strict evidence hierarchy and polished loop."
    - "D-69, D-74, D-75, D-76: Persistence and loop behavior require focused tests and evidence before the phase can be complete."
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Normalized protocol revision and real-match transfer tables while preserving existing coach_protocol_outcomes."
      contains: "completeTrainingProtocolRevisions"
    - path: "src/actions/history.ts"
      provides: "Save-time protocol snapshot, revision action, extended outcome normalization, compatible validation prompts, and real-match transfer action."
      contains: "recordCompleteTrainingProtocolRevision"
    - path: "src/core/coach-outcomes.ts"
      provides: "Extended Phase 8 outcome statuses and evidence mapping for fatigue/confusion/variable change."
      contains: "fatigue_or_pain"
  key_links:
    - from: "src/actions/history.ts"
      to: "src/core/training-protocols.ts"
      via: "saveAnalysisResult persists completeProtocol snapshot after deterministic coach plan is built"
      pattern: "completeProtocol"
    - from: "src/core/coach-outcomes.ts"
      to: "src/core/coach-memory.ts"
      via: "new Phase 8 outcome statuses continue feeding outcome memory conservatively"
      pattern: "variable_changed"
---

<objective>
Persist complete protocol snapshots and add revision, outcome, compatible-validation, and real-match transfer records.

Purpose: make Phase 8 protocols auditable over time without mutating old analyses or treating self-report/transfer as stronger evidence than compatible clips.
Output: schema/migration updates, save-time snapshot persistence, revision action, extended outcome statuses, transfer action, hydration support, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-complete-training-protocols/08-CONTEXT.md
@.planning/phases/08-complete-training-protocols/08-RESEARCH.md
@.planning/phases/08-complete-training-protocols/08-01-SUMMARY.md
@src/types/engine.ts
@src/db/schema.ts
@src/db/schema.test.ts
@src/core/coach-outcomes.ts
@src/core/coach-memory.ts
@src/actions/history.ts
@src/actions/history.test.ts
@src/app/history/analysis-result-hydration.ts
</context>

<threat_model>
Scope: persistence, snapshots, revisions, outcome semantics, compatible validation prompts, and transfer records.

Threats:
- HIGH: future drill catalog changes silently rewrite old protocol truth. Mitigation: save complete protocol snapshot inside stored `fullResult` and revision rows reference old/new snapshot payloads.
- HIGH: real-match transfer or self-report promotes technical truth. Mitigation: transfer record has `countsAsTechnicalValidation: false`; compatible clip remains the only strong technical validation path.
- HIGH: pain/fatigue is stored as sensitive health history or counted as aim failure. Mitigation: persist only reason/status/note and conservative flags; memory maps it to safety/learning downgrade.
- MEDIUM: schema changes break old history hydration. Mitigation: new fields are optional and hydration tests cover legacy results.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add schema for protocol revisions and transfer records</name>
  <files>src/db/schema.ts, drizzle/0011_complete_training_protocols.sql, drizzle/meta/_journal.json, src/db/schema.test.ts</files>
  <read_first>src/db/schema.ts, src/db/schema.test.ts, drizzle/0009_coach_protocol_outcomes.sql, drizzle/0010_freemium_pro_mvp.sql, drizzle/meta/_journal.json</read_first>
  <action>
Add two tables in `src/db/schema.ts`:

1. `completeTrainingProtocolRevisions`
- `id uuid primary key`
- `userId text not null references users.id cascade`
- `analysisSessionId uuid not null references analysisSessions.id cascade`
- `coachPlanId text not null`
- `protocolId text not null`
- `revisionReason text not null`
- `tierDirection text not null` constrained in code to `stronger | same | more_conservative`
- `changedFields jsonb not null`
- `previousProtocol jsonb not null`
- `revisedProtocol jsonb not null`
- `evidencePayload jsonb not null`
- `createdAt timestamp not null default now`

2. `trainingProtocolTransferRecords`
- `id uuid primary key`
- `userId text not null references users.id cascade`
- `analysisSessionId uuid not null references analysisSessions.id cascade`
- `protocolId text not null`
- `situation text not null`
- `weaponId text`
- `opticId text`
- `approximateDistanceMeters integer`
- `pressureLevel text not null`
- `feltControl text not null`
- `result text not null`
- `note text`
- `countsAsTechnicalValidation boolean not null default false`
- `createdAt timestamp not null default now`

Add relations and infer types. Add `drizzle/0011_complete_training_protocols.sql` with matching SQL and indexes:
- `complete_training_protocol_revisions_user_session_idx`
- `complete_training_protocol_revisions_protocol_idx`
- `training_protocol_transfer_records_user_session_idx`
- `training_protocol_transfer_records_protocol_idx`

Update `drizzle/meta/_journal.json` consistently with the new migration.
  </action>
  <verify>npx vitest run src/db/schema.test.ts</verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `completeTrainingProtocolRevisions` and `trainingProtocolTransferRecords`.
    - `drizzle/0011_complete_training_protocols.sql` contains `CREATE TABLE IF NOT EXISTS "complete_training_protocol_revisions"`.
    - Schema test asserts transfer records have `counts_as_technical_validation`.
    - Schema test asserts revision rows include `previous_protocol` and `revised_protocol`.
  </acceptance_criteria>
  <done>Database can audit protocol revisions and transfer records without changing old coach outcome rows.</done>
</task>

<task type="auto">
  <name>Task 2: Persist snapshots and explicit revisions</name>
  <files>src/actions/history.ts, src/actions/history.test.ts, src/app/history/analysis-result-hydration.ts, src/app/history/analysis-result-hydration.test.ts</files>
  <read_first>src/actions/history.ts, src/actions/history.test.ts, src/app/history/analysis-result-hydration.ts, src/core/training-protocols.ts, src/types/engine.ts, src/db/schema.ts</read_first>
  <action>
Update `saveAnalysisResult` so the stored `fullResult` keeps `coachPlan.completeProtocol` from Plan 08-01.

Add `recordCompleteTrainingProtocolRevision(input)` server action in `src/actions/history.ts`:
- requires authenticated user;
- validates session ownership;
- reads current stored `fullResult.coachPlan.completeProtocol`;
- accepts `revisionReason`, `changedFields`, `revisedProtocol`, and `evidencePayload`;
- rejects if `revisedProtocol.version !== 'complete-protocol-v1'`;
- computes `tierDirection` by comparing previous/revised protocol tier order: `capture_again < test_protocol < stabilize_block < apply_protocol`;
- inserts into `completeTrainingProtocolRevisions`;
- revalidates `/history`, `/history/{sessionId}`, and `/dashboard`;
- returns `{ success: true, revision }` or `{ success: false, error }`.

Update hydration so old results without `completeProtocol` still hydrate and expose `coachPlan` normally.
  </action>
  <verify>npx vitest run src/actions/history.test.ts src/app/history/analysis-result-hydration.test.ts</verify>
  <acceptance_criteria>
    - `saveAnalysisResult` tests prove saved `fullResult.coachPlan.completeProtocol.version` is `complete-protocol-v1`.
    - Revision action tests reject unauthenticated users and wrong-session users.
    - Revision action tests insert `tierDirection: 'more_conservative'` when revised tier is weaker.
    - Hydration tests pass for a legacy `coachPlan` without `completeProtocol`.
  </acceptance_criteria>
  <done>Protocol snapshots and revisions are auditable and old reports remain readable.</done>
</task>

<task type="auto">
  <name>Task 3: Extend outcomes, compatible validation, and transfer records</name>
  <files>src/types/engine.ts, src/core/coach-outcomes.ts, src/core/coach-outcomes.test.ts, src/actions/history.ts, src/actions/history.test.ts</files>
  <read_first>src/types/engine.ts, src/core/coach-outcomes.ts, src/core/coach-outcomes.test.ts, src/core/coach-memory.ts, src/actions/history.ts, src/actions/history.test.ts</read_first>
  <action>
Extend `CoachProtocolOutcomeStatus` with exact new values:
- `fatigue_or_pain`
- `confused`
- `variable_changed`

Update `COACH_PROTOCOL_OUTCOME_STATUSES`, normalization, and `resolveCoachOutcomeEvidence`:
- `fatigue_or_pain` => `evidenceStrength: 'invalid'`, `countsAsTechnicalEvidence: false`, `needsCompatibleValidation: true`, `invalidBecauseOfExecutionOrCapture: true`.
- `confused` => `evidenceStrength: 'neutral'`, `countsAsTechnicalEvidence: false`, `needsCompatibleValidation: true`.
- `variable_changed` => `evidenceStrength: 'invalid'`, `countsAsTechnicalEvidence: false`, `needsCompatibleValidation: true`, `invalidBecauseOfExecutionOrCapture: true`.

Add `recordTrainingProtocolTransfer(input)` in `src/actions/history.ts`:
- requires authenticated user and owned session;
- validates `situation`, `pressureLevel`, `feltControl`, and `result` as short strings;
- stores `countsAsTechnicalValidation: false` always;
- returns the record and never updates precision trend as technical proof.

Add `buildCompatibleValidationChecklistFromProtocol(protocol)` helper in `history.ts` or a small core helper that returns weapon, optic, distance/range, stance, attachments, sensitivity, preparation, duration, and success criterion from `completeProtocol.validation`.
  </action>
  <verify>npx vitest run src/core/coach-outcomes.test.ts src/actions/history.test.ts</verify>
  <acceptance_criteria>
    - `CoachProtocolOutcomeStatus` contains `fatigue_or_pain`, `confused`, and `variable_changed`.
    - Outcome tests prove `fatigue_or_pain` and `variable_changed` do not count as technical evidence.
    - Transfer action tests prove `countsAsTechnicalValidation` is always false even when result is improved.
    - Compatible validation helper returns at least weapon, optic, distance, stance, sensitivity, duration, and success criterion.
  </acceptance_criteria>
  <done>The loop can record outcome, repair, compatible validation, and practical transfer without overstating evidence.</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/db/schema.test.ts src/core/coach-outcomes.test.ts src/actions/history.test.ts src/app/history/analysis-result-hydration.test.ts
- [ ] npm run typecheck
</verification>

<success_criteria>

- Saved analyses preserve a versioned complete protocol snapshot.
- Protocol revisions are explicit, owned, auditable, and compare old/new tier direction.
- Outcome statuses distinguish fatigue/pain, confusion, variable change, invalid capture, and real technical evidence.
- Compatible clip validation remains strict technical proof; real-match transfer is practical but conservative.
- Legacy history remains readable.

</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-training-protocols/08-02-SUMMARY.md` with schema changes, action contracts, migration evidence, and focused test results.
</output>
