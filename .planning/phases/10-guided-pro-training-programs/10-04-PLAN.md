---
phase: 10-guided-pro-training-programs
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - src/actions/dashboard.ts
  - src/actions/dashboard-active-coach-loop.ts
  - src/actions/dashboard.test.ts
  - src/app/dashboard/dashboard-truth-view-model.ts
  - src/app/dashboard/page.tsx
  - src/app/dashboard/page.contract.test.ts
  - src/app/analyze/results-dashboard-view-model.ts
  - src/app/analyze/results-dashboard.tsx
  - src/app/analyze/results-dashboard-view-model.test.ts
  - src/app/analyze/results-dashboard.contract.test.ts
autonomous: true
requirements: [PREC-03, COACH-01, COACH-02, COACH-03, COACH-04, COACH-05, MON-01, MON-02]
user_setup: []
must_haves:
  truths:
    - "D-32, D-33, D-34, D-35, D-36, D-37, D-38, D-39, D-40, D-41, D-42, D-43, D-44: Dashboard/result handoffs must respect repair, consolidation, line restart, fatigue, discomfort stop, confusion, and low fidelity."
    - "D-45, D-46, D-47, D-48, D-49, D-50: Missed days and inconsistent use reenter with evidence-preserving copy, not punishment."
    - "D-51, D-52, D-53, D-54, D-55, D-56, D-57, D-58: Stable program states and adaptation reasons drive dashboard CTA, result entry, and UI copy."
    - "D-67, D-68, D-69, D-70, D-71, D-72, D-73, D-74: Dashboard is now-cockpit, program route is full map, history is audit, Analyze is validation, Spray Lab is execution."
    - "D-75, D-76, D-77, D-78: Surface copy uses premium system language and frames repair/recovery as coaching, not punishment."
  artifacts:
    - path: "src/actions/dashboard.ts"
      provides: "Dashboard data includes active Ciclo Pro projection."
      contains: "activeTrainingProgram"
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard now-cockpit for current Ciclo Pro mission and blocker."
      contains: "Abrir Ciclo Pro"
    - path: "src/app/analyze/results-dashboard-view-model.ts"
      provides: "Post-analysis entry into Ciclo Pro or Ciclo de Reparo."
      contains: "buildTrainingProgramHref"
  key_links:
    - from: "src/actions/training-programs.ts"
      to: "src/actions/dashboard.ts"
      via: "dashboard loads active program summary"
      pattern: "getActiveTrainingProgramCycleAction"
    - from: "src/app/analyze/results-dashboard-view-model.ts"
      to: "src/actions/training-programs.ts"
      via: "result CTA starts or opens Ciclo Pro from saved analysis"
      pattern: "createTrainingProgramCycleAction"
---

<objective>
Integrate Ciclo Pro into the active product loop: dashboard cockpit, result entry, Spray Lab/Analyze handoffs, and reentry copy.

Expected output: users see the current mission and program state where they act, while full detail remains on `/ciclo-pro`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-guided-pro-training-programs/10-CONTEXT.md
@.planning/phases/10-guided-pro-training-programs/10-RESEARCH.md
@src/actions/dashboard.ts
@src/actions/dashboard-active-coach-loop.ts
@src/actions/training-programs.ts
@src/lib/training-program-projection.ts
@src/app/dashboard/page.tsx
@src/app/dashboard/dashboard-truth-view-model.ts
@src/app/analyze/results-dashboard-view-model.ts
@src/app/analyze/results-dashboard.tsx
@src/app/spray-lab/spray-lab-view-model.ts
@src/app/analyze/page.tsx
</context>

<threat_model>
Scope: dashboard data/model/UI, result CTAs, active handoffs.

Threats:
- HIGH: dashboard becomes a full report and hides the current action. Mitigation: show only week, dominant mission, state, evidence, blocker, CTA.
- HIGH: result starts full Pro cycle from weak evidence. Mitigation: result route uses eligibility and may start Ciclo de Reparo.
- HIGH: handoffs trust client query params for program truth. Mitigation: actions reload owned program/Lab/analysis rows before changes.
- MEDIUM: missed-day copy shames the user. Mitigation: reentry copy says the cycle was re-fitted to preserve evidence.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add active program data to dashboard action</name>
  <files>src/actions/dashboard.ts, src/actions/dashboard.test.ts</files>
  <read_first>src/actions/dashboard.ts, src/actions/training-programs.ts, src/lib/training-program-projection.ts</read_first>
  <action>
Extend `DashboardStats` with a compact active program summary:
- cycle id/kind/state;
- current week label;
- current mission title/category;
- visible adaptation reason;
- blocker count;
- evidence status;
- primary CTA label/href;
- Free/Pro lock state.

Load the active cycle server-side and project it using product access. Keep null behavior safe for users without programs.
  </action>
  <verify>npx vitest run src/actions/dashboard.test.ts</verify>
  <acceptance_criteria>
    - Anonymous/no-program dashboard still works.
    - Free dashboard shows one basic mission and honest upgrade CTA.
    - Pro dashboard shows current mission and state without full map payload.
    - Weak/repair/consolidation/validation states produce visible reasons.
  </acceptance_criteria>
  <done>Dashboard action can command Ciclo Pro now-state.</done>
</task>

<task type="auto">
  <name>Task 2: Render dashboard Ciclo Pro cockpit</name>
  <files>src/app/dashboard/dashboard-truth-view-model.ts, src/app/dashboard/page.tsx, src/app/dashboard/page.contract.test.ts</files>
  <read_first>src/app/dashboard/page.tsx, src/ui/components/page-command-header.tsx, src/ui/components/loop-rail.tsx</read_first>
  <action>
Add compact dashboard UI:
- `Semana N de 4`;
- current mission;
- program state;
- evidence/reason;
- blocker;
- CTA to `/ciclo-pro`, `/spray-lab`, or `/analyze?mode=validation`;
- reentry copy for missed/stale/variable-changed states.

Dashboard must remain the cockpit of now, not the full route.
  </action>
  <verify>npx vitest run src/app/dashboard/page.contract.test.ts</verify>
  <acceptance_criteria>
    - `Abrir Ciclo Pro` is visible when program state exists or can be started.
    - Dashboard does not render all four weeks or full audit.
    - Blockers and adaptation reasons stay visible.
    - UI avoids punishment language for missed days or repair.
  </acceptance_criteria>
  <done>Dashboard points the user to the current program action.</done>
</task>

<task type="auto">
  <name>Task 3: Add post-analysis Ciclo Pro entry points</name>
  <files>src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard.tsx, src/app/analyze/results-dashboard-view-model.test.ts, src/app/analyze/results-dashboard.contract.test.ts</files>
  <read_first>src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/complete-training-protocol-view-model.ts, src/lib/training-program-projection.ts</read_first>
  <action>
Add result CTAs and cards:
- usable saved analysis can open/start Ciclo Pro;
- weak saved analysis can open/start Ciclo de Reparo;
- unsaved analysis asks user to save first;
- result card explains Free next step vs Pro full cycle;
- do not hide truth evidence or blockers.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard-view-model.test.ts src/app/analyze/results-dashboard.contract.test.ts</verify>
  <acceptance_criteria>
    - Result CTA is disabled/null before save when server action cannot own the analysis.
    - Weak evidence points to repair cycle, not fake Pro progress.
    - Copy sells adaptive/auditable program continuity without guaranteed improvement.
  </acceptance_criteria>
  <done>Analysis results can enter the program loop honestly.</done>
</task>

<task type="auto">
  <name>Task 4: Wire program handoffs to Spray Lab and Analyze validation</name>
  <files>src/actions/dashboard-active-coach-loop.ts, src/app/analyze/results-dashboard-view-model.ts, src/app/dashboard/page.tsx, src/actions/dashboard.test.ts</files>
  <read_first>src/core/spray-lab-coach-handoff.ts, src/app/spray-lab/spray-lab-view-model.ts, src/app/analyze/page.tsx</read_first>
  <action>
Ensure program mission CTAs use existing execution/validation paths:
- execution mission -> `/spray-lab?sourceSessionId=...&protocolId=...` or lab session id;
- validation mission -> `/analyze?mode=validation&labSessionId=...&protocolId=...`;
- repair mission -> relevant Analyze/Spray Lab reentry;
- completed/technical checkpoint -> `/ciclo-pro` or history audit.
  </action>
  <verify>npx vitest run src/actions/dashboard.test.ts src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - No new video analysis path is added to program route/dashboard.
    - Handoff hrefs preserve owned IDs and let actions validate ownership.
    - Practical transfer remains separate from technical compatible validation in copy.
  </acceptance_criteria>
  <done>Ciclo Pro controls the loop without duplicating execution or validation.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/actions/dashboard.test.ts src/app/dashboard/page.contract.test.ts src/app/analyze/results-dashboard-view-model.test.ts src/app/analyze/results-dashboard.contract.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Dashboard shows compact active Ciclo Pro now-state.
- Result surfaces can start/open Ciclo Pro or Ciclo de Reparo from saved analyses.
- Program CTAs route to existing Spray Lab/Analyze validation paths.
- Missed-day/reentry/repair copy preserves evidence and avoids punishment.
- Dashboard remains concise while `/ciclo-pro` owns full map depth.
</success_criteria>

<output>
Ciclo Pro integrated into dashboard and result action loops.
</output>
