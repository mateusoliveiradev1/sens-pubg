---
phase: 10-guided-pro-training-programs
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/db/schema.ts
  - drizzle/0013_guided_pro_training_programs.sql
  - src/actions/training-programs.ts
  - src/actions/training-programs.test.ts
  - src/lib/product-entitlements.ts
  - src/lib/premium-projection.ts
  - src/lib/training-program-projection.ts
  - src/lib/training-program-projection.test.ts
autonomous: true
requirements: [PREC-03, COACH-01, COACH-02, COACH-03, COACH-04, COACH-05, MON-01, MON-02]
user_setup:
  - "After implementation, apply and verify `drizzle/0013_guided_pro_training_programs.sql` in the configured target database before declaring Phase 10 fully Delivered."
must_haves:
  truths:
    - "D-09, D-10, D-11, D-12, D-13, D-14, D-15: Actions must create full Ciclo Pro only from usable owned evidence, create Ciclo de Reparo from weak bases, and archive old lines on structural restart."
    - "D-51, D-52, D-53, D-54, D-55, D-56, D-57, D-58: Stable technical states/reason codes must drive persistence, projection, UI, coach handoff, and tests."
    - "D-59, D-60, D-61, D-62, D-63, D-64, D-65, D-66: Free is useful and honest; Pro unlocks full 30-day cycle depth; value is original Sens PUBG value and never PUBG API-exclusive or guaranteed improvement."
    - "D-79, D-80, D-81, D-82, D-83: Persistence/projection must be represented in the Phase 10 verifier and final evidence matrix."
  artifacts:
    - path: "drizzle/0013_guided_pro_training_programs.sql"
      provides: "Database migration for program cycles, weeks, missions, checkpoints, and events."
      contains: "training_program_cycles"
    - path: "src/actions/training-programs.ts"
      provides: "Authenticated server actions for creating, loading, reentering, advancing, and checkpointing Ciclo Pro."
      contains: "createTrainingProgramCycleAction"
    - path: "src/lib/training-program-projection.ts"
      provides: "Server-owned Free/Pro projection for Ciclo Pro."
      contains: "projectTrainingProgramForAccess"
  key_links:
    - from: "src/types/monetization.ts"
      to: "src/lib/product-entitlements.ts"
      via: "programs.guided_weekly and programs.guided_monthly become Pro-granted features"
      pattern: "programs.guided_monthly"
    - from: "src/core/training-programs.ts"
      to: "src/actions/training-programs.ts"
      via: "server actions persist deterministic reducer outputs"
      pattern: "reduceTrainingProgramEvent"
---

<objective>
Persist Ciclo Pro and make access server-owned: schema, actions, entitlement grants, Free/Pro projection, and ownership checks.

Expected output: the product can create/load/update program cycles safely, Free sees an honest basic mission, and Pro receives the full 30-day adaptive map.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-guided-pro-training-programs/10-CONTEXT.md
@.planning/phases/10-guided-pro-training-programs/10-RESEARCH.md
@src/types/training-programs.ts
@src/core/training-programs.ts
@src/db/schema.ts
@drizzle/0012_spray_lab_sessions.sql
@src/actions/spray-lab.ts
@src/actions/history.ts
@src/lib/product-entitlements.ts
@src/lib/premium-projection.ts
@src/lib/spray-lab-projection.ts
@src/types/monetization.ts
</context>

<threat_model>
Scope: database schema, server actions, product entitlements, and projection.

Threats:
- HIGH: client payload grants Pro program access or advances a cycle. Mitigation: actions load owned evidence and use server-side product access.
- HIGH: program rows store raw video, health/medical detail, or private notes beyond reason codes. Mitigation: normalized columns plus versioned snapshots only.
- HIGH: Free uses fake locked data or deceptive blur. Mitigation: projection returns real next step, real blocker, and one basic mission only.
- HIGH: program entitlements accidentally gate truth evidence. Mitigation: Free keeps confidence, coverage, blockers, and basic next step visible.
- MEDIUM: migration exists but is not applied. Mitigation: user_setup and final verifier keep Delivered blocked until target DB is verified.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add program persistence schema and migration</name>
  <files>src/db/schema.ts, drizzle/0013_guided_pro_training_programs.sql</files>
  <read_first>src/db/schema.ts, drizzle/0012_spray_lab_sessions.sql, src/types/training-programs.ts</read_first>
  <action>
Add normalized tables:
- `trainingProgramCycles`;
- `trainingProgramWeeks`;
- `trainingProgramMissions`;
- `trainingProgramCheckpoints`;
- `trainingProgramEvents`.

Include user ownership, base analysis/session references, active line/context key, program kind, state, week number, mission slot/category/status, checkpoint layer/outcome, stable reason codes, evidence references to Spray Lab/validation/protocol revisions, timestamps, and versioned JSON snapshots.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Migration matches Drizzle schema naming and index style.
    - Queries can load active cycle by user, current week, current mission, checkpoints, and events.
    - No raw video, medical history, detailed pain log, body metrics, or sensitive physical routine detail is stored.
    - Schema test expectations are updated if this repo has schema coverage for new tables.
  </acceptance_criteria>
  <done>Database can store Ciclo Pro without privacy creep.</done>
</task>

<task type="auto">
  <name>Task 2: Create authenticated training program actions</name>
  <files>src/actions/training-programs.ts, src/actions/training-programs.test.ts</files>
  <read_first>src/actions/spray-lab.ts, src/actions/history.ts, src/core/training-programs.ts, src/db/schema.ts</read_first>
  <action>
Implement actions:
- `createTrainingProgramCycleAction`;
- `getActiveTrainingProgramCycleAction`;
- `getTrainingProgramCycleAction`;
- `reenterTrainingProgramCycleAction`;
- `completeTrainingProgramMissionAction`;
- `closeTrainingProgramWeekAction`;
- `recordTrainingProgramCheckpointAction`;
- `pauseTrainingProgramCycleAction`.

Each action must authenticate, check ownership, load owned analysis/protocol/Lab/validation evidence when needed, reject invalid transitions, persist reducer output, and revalidate `/ciclo-pro`, `/dashboard`, `/history`, and related detail routes.
  </action>
  <verify>npx vitest run src/actions/training-programs.test.ts</verify>
  <acceptance_criteria>
    - User cannot load or mutate another user's program.
    - Client cannot mark technical progress without compatible validation evidence.
    - Mission completion records reason/evidence references and cannot skip required repair blockers.
    - Reentry handles missed days, stale context, and variable change with honest state.
  </acceptance_criteria>
  <done>Server owns the Ciclo Pro lifecycle.</done>
</task>

<task type="auto">
  <name>Task 3: Grant and project program entitlements</name>
  <files>src/lib/product-entitlements.ts, src/lib/premium-projection.ts, src/lib/training-program-projection.ts, src/lib/training-program-projection.test.ts</files>
  <read_first>src/types/monetization.ts, src/lib/product-entitlements.ts, src/lib/spray-lab-projection.ts, src/lib/premium-projection.ts</read_first>
  <action>
Add `programs.guided_weekly` and `programs.guided_monthly` to the active Pro entitlement set and premium feature copy.

Create `projectTrainingProgramForAccess` with:
- Free: next step, one basic weekly mission, blockers/evidence/reason, CTA to Ciclo Pro, no fake data.
- Pro: full 30-day cycle, four weeks, all missions, checkpoints, recovery, history/comparison/audit, active-line continuity.
  </action>
  <verify>npx vitest run src/lib/training-program-projection.test.ts src/lib/product-entitlements.test.ts src/lib/premium-projection.test.ts</verify>
  <acceptance_criteria>
    - Free projection never receives full Pro audit or fake hidden missions.
    - Pro projection grants weekly/monthly program depth only from server-side access.
    - Lock copy says Free shows the next step and Pro organizes the complete adaptive/auditable cycle.
    - Copy does not sell PUBG API-derived data or guarantee improvement.
  </acceptance_criteria>
  <done>Program monetization cut is explicit and honest.</done>
</task>

<task type="auto">
  <name>Task 4: Persist projection-ready snapshots</name>
  <files>src/actions/training-programs.ts, src/actions/training-programs.test.ts</files>
  <read_first>src/lib/training-program-projection.ts, src/core/training-program-checkpoints.ts</read_first>
  <action>
Ensure actions persist enough snapshot data for fast route/dashboard/history projection:
- current cycle state;
- current week and mission;
- latest checkpoint;
- active line/context;
- visible adaptation reason;
- blocker summary;
- related Spray Lab/Analyze links.

Avoid recomputing from long history on every route load when a durable snapshot is enough.
  </action>
  <verify>npx vitest run src/actions/training-programs.test.ts src/lib/training-program-projection.test.ts</verify>
  <acceptance_criteria>
    - Active cycle load returns a projection-ready snapshot.
    - Completed/archived cycle still has monthly checkpoint and old line reference.
    - Stale/missed-day reentry reason is visible.
  </acceptance_criteria>
  <done>Routes can render program state without trusting clients.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/actions/training-programs.test.ts src/lib/training-program-projection.test.ts src/lib/product-entitlements.test.ts src/lib/premium-projection.test.ts`
- `npm run typecheck`
- Apply/verify `drizzle/0013_guided_pro_training_programs.sql` in the target DB before final Delivered status.
</verification>

<success_criteria>
- Program cycle/week/mission/checkpoint/event tables exist with ownership and indexes.
- Server actions own all lifecycle transitions.
- Free/Pro program projection is tested and honest.
- Program entitlement keys are granted to Pro and projected into locks/copy.
- Persistence does not store raw video or sensitive health details.
</success_criteria>

<output>
Ciclo Pro storage, server actions, and access projection ready for route/UI work.
</output>
