---
phase: 01-measurement-truth-contract
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/analyze/results-dashboard.tsx
  - src/app/analyze/results-dashboard-view-model.ts
  - src/app/analyze/results-dashboard-view-model.test.ts
  - src/app/analyze/results-dashboard.contract.test.ts
  - src/app/analyze/analysis.module.css
  - src/app/copy-claims.contract.test.ts
autonomous: true
requirements: [PREC-01, PREC-02, PREC-04]
user_setup: []
must_haves:
  truths:
    - "D-18: The analysis result shows the recommended action first, with mastery score and pillars directly below."
    - "D-19: The current analysis result experience becomes a premium clip report/coach rather than stacked laudo cards."
    - "D-21: Analysis and dashboard share visual language but analysis remains a clip-specific report."
    - "D-24: First fold after clip analysis contains verdict plus next block, action state, mastery score, mechanical level, and training block."
    - "D-25: Diagnosis, sensitivity, coach, and evidence are organized as what happened, why it matters, what to train, and how to validate."
    - "D-26: Technical evidence remains available in a secondary audit layer."
    - "D-27: Visual feel is premium, clean, decisive, and evidence proves decisions rather than decorating."
    - "D-39: The report uses the shared primary state taxonomy."
    - "D-40: The report uses the shared mechanical level taxonomy."
    - "D-41: The report uses friendly diagnosis labels instead of primary technical codes."
    - "D-42: Report copy is short, direct, premium, and never exaggerated."
  artifacts:
    - path: "src/app/analyze/results-dashboard.tsx"
      provides: "Verdict-first premium analysis report consuming AnalysisResult.mastery."
      contains: "mastery"
    - path: "src/app/analyze/results-dashboard-view-model.ts"
      provides: "Presentation helpers for mastery verdict, pillars, evidence badges, and report copy."
    - path: "src/app/analyze/results-dashboard.contract.test.ts"
      provides: "Contract tests that the report renders verdict, next block, pillars, visualization, and audit evidence in order."
  key_links:
    - from: "src/app/analyze/results-dashboard.tsx"
      to: "src/types/engine.ts"
      via: "ResultsDashboard reads AnalysisResult.mastery from the shared contract"
      pattern: "activeSession.mastery"
    - from: "src/app/analyze/results-dashboard.tsx"
      to: "src/core/measurement-truth.ts"
      via: "UI uses friendly labels generated by shared truth helpers or stored mastery"
      pattern: "formatDiagnosisTruthLabel|mastery"
    - from: "src/app/analyze/results-dashboard.tsx"
      to: "src/app/analyze/spray-visualization"
      via: "verdict-first report keeps spray visualization immediately after the next-block proof section"
      pattern: "SprayVisualization"
---

<objective>
Redesign the clip analysis result into a verdict-first premium report that consumes the shared mastery contract.

Purpose: make the result immediately answer what action the user should take, why the product trusts or downgrades that action, and what to train next, while preserving all existing audit evidence below.
Output: `ResultsDashboard` hierarchy, view-model helpers, CSS, and contract tests updated around mastery/action/pillars.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-truth-contract/01-CONTEXT.md
@.planning/phases/01-measurement-truth-contract/01-RESEARCH.md
@.planning/phases/01-measurement-truth-contract/01-01-SUMMARY.md
@src/app/analyze/results-dashboard.tsx
@src/app/analyze/results-dashboard-view-model.ts
@src/app/analyze/analysis.module.css
@src/app/analyze/results-dashboard.contract.test.ts
@src/app/analyze/results-dashboard-view-model.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the verdict-first report model</name>
  <files>src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard-view-model.test.ts</files>
  <read_first>src/app/analyze/results-dashboard-view-model.ts, src/types/engine.ts, src/core/measurement-truth.ts, src/app/analyze/tracking-summary.ts</read_first>
  <action>
Extend the results view model with helpers that turn `AnalysisResult.mastery`, `CoachPlan`, and tracking evidence into compact report data.

Add exported helpers such as:
- `buildMasteryPillarCards(mastery)` returning labels `Controle`, `Consistencia`, `Confianca`, `Qualidade do clip`, values, tone, and short reason.
- `buildResultVerdictModel({ mastery, coachPlan, trackingOverview, sensitivity })` returning action label, mechanical level label, actionable score, mechanical score, primary explanation, blocked/downgraded reasons, and next block summary.
- `buildEvidenceBadges({ mastery, trackingOverview })` returning confidence, coverage, visible/lost frames, inconclusive/capture state, and clip quality labels.

Use the shared `formatDiagnosisTruthLabel` for diagnosis names rather than hard-coded uppercase technical labels.

Tests must prove:
- weak evidence produces a conservative verdict model with blocked recommendations;
- testable evidence produces `Testavel` with a next-block summary;
- ready evidence still uses validation language, not final/perfect language;
- pillar cards include all four pillars in the required order.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - `buildResultVerdictModel` or equivalent is exported.
    - Tests assert the four pillar labels in order.
    - Tests assert weak evidence includes `Capturar de novo` or `Incerto`.
    - Tests assert no verdict copy contains `perfeito`, `garantido`, or `definitivo`.
  </acceptance_criteria>
  <done>The report view model can render Phase 1 truth without reaching into raw engine internals from JSX.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ResultsDashboard hierarchy around mastery</name>
  <files>src/app/analyze/results-dashboard.tsx, src/app/analyze/analysis.module.css</files>
  <read_first>src/app/analyze/results-dashboard.tsx, src/app/analyze/analysis.module.css, src/app/analyze/spray-visualization.tsx, src/app/analyze/results-dashboard.contract.test.ts</read_first>
  <action>
Refactor `ResultsDashboard` so the first visible report section after sub-session selector is:

1. A verdict header using `activeSession.mastery.actionLabel`, `actionableScore`, and `mechanicalLevelLabel`.
2. A next-block panel using `activeSession.coachPlan.nextBlock` when available, with duration, first 2-3 steps, and the first validation check.
3. Evidence badges for confidence, coverage, visible frames, lost frames, clip quality, and inconclusive/capture blocks.
4. Four mastery pillar blocks directly below: control, consistency, confidence, clip quality.
5. `SprayVisualization` immediately after the verdict/pillars as proof.

Keep the existing video quality, tracking timeline, metric cards, sensitivity profile cards, coach plan details, and grouped coach evidence, but move them below the verdict/proof layer in the narrative order:
- what happened,
- why it matters,
- what to train,
- how to validate,
- audit evidence.

Replace primary visible technical labels from `DIAG_META`:
- `UNDERPULL` -> `Spray subindo`
- `OVERPULL` -> `Spray descendo demais`
- `JITTER` -> `Tremida lateral`
- `H. DRIFT` -> `Desvio lateral`
- `INCONSISTENCY` -> `Spray irregular`
- `LATE RESPONSE` -> `Reacao atrasada`

CSS requirements:
- Add stable responsive containers for verdict, next block, evidence badges, and pillars.
- Keep card radius at 8px or less unless existing local classes force a different radius.
- Avoid nested cards inside cards; the verdict section should be a full report band or a single framed tool surface, not multiple floating card layers inside another card.
- Ensure mobile text wraps without overlap and avoids viewport-scaled fonts.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard.contract.test.ts src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - `ResultsDashboard` reads `activeSession.mastery`.
    - Verdict content appears before `SprayVisualization` and before detailed sensitivity profile cards.
    - `ResultsDashboard` still renders `trackingTimeline.segments.map`, `videoQualityTimeline`, and `coachPlan.nextBlock`.
    - Primary visible labels no longer include uppercase `UNDERPULL`, `OVERPULL`, `JITTER`, `H. DRIFT`, or `INCONSISTENCY`.
  </acceptance_criteria>
  <done>Analysis results lead with a decisive but honest clip report and keep existing evidence available below.</done>
</task>

<task type="auto">
  <name>Task 3: Protect report copy and UI contracts</name>
  <files>src/app/analyze/results-dashboard.contract.test.ts, src/app/copy-claims.contract.test.ts</files>
  <read_first>src/app/analyze/results-dashboard.contract.test.ts, src/app/copy-claims.contract.test.ts, src/app/analyze/results-dashboard.tsx</read_first>
  <action>
Update contract tests to lock the Phase 1 report behavior:

- `results-dashboard.contract.test.ts` must assert the report includes `actionLabel`/`mastery`, four pillar labels, next block, confidence, coverage, visible/lost frames, and `SprayVisualization` after the verdict.
- Add assertions that the detailed audit evidence still exists: video quality, tracking timeline, diagnosis details, sensitivity tier/evidence, and coach validation check.
- Add assertions that report copy uses protocol/validation language and does not claim perfect sensitivity, guaranteed improvement, or final player skill.
- Add `src/app/analyze/results-dashboard.tsx` to `PRODUCT_COPY_FILES` in `copy-claims.contract.test.ts` if the report contains enough user-facing copy to deserve claim protection.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard.contract.test.ts src/app/copy-claims.contract.test.ts</verify>
  <acceptance_criteria>
    - Contract tests fail if the verdict section stops reading `mastery`.
    - Contract tests fail if confidence/coverage/visible/lost frame evidence disappears.
    - Copy contract scans the report or an equivalent report-copy source for banned claims.
  </acceptance_criteria>
  <done>The premium report shape and claim guardrails are regression-protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/app/analyze/results-dashboard-view-model.test.ts src/app/analyze/results-dashboard.contract.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- Analysis result first fold shows action state, mastery score, mechanical level, next block, evidence, and pillars.
- Technical tracking/video/diagnosis/sensitivity evidence remains available below the primary report.
- User-facing report copy uses test-protocol language and avoids perfect/guaranteed claims.
- Friendly diagnosis labels replace primary technical labels in the visible report.

</success_criteria>

<output>
After completion, create `.planning/phases/01-measurement-truth-contract/01-02-SUMMARY.md`
</output>
