---
phase: 09-pubg-spray-lab-session-runner-and-benchmark
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/db/schema.ts
  - drizzle/0012_spray_lab_sessions.sql
  - src/actions/spray-lab.ts
  - src/actions/spray-lab.test.ts
  - src/lib/product-entitlements.ts
  - src/lib/premium-projection.ts
  - src/lib/spray-lab-projection.ts
  - src/lib/spray-lab-projection.test.ts
autonomous: true
requirements: [PREC-03, BENCH-01, BENCH-02, BENCH-03, COACH-01, COACH-02, COACH-03]
user_setup:
  - "Depois da implementacao, aplicar/verificar `drizzle/0012_spray_lab_sessions.sql` no banco configurado antes de declarar entrega completa."
must_haves:
  truths:
    - "D-46, D-47, D-48: Spray Lab must persist sessions/events/snapshots/validation links with minimal evidence and no raw Lab video."
    - "D-49, D-50, D-51, D-52: Free gets a useful guided Lab path; Pro gets deeper runner, benchmarks, audit, comparisons, and continuity through server-owned entitlements."
    - "D-53, D-54: Private Lab data can inform coach hierarchy, but must not create global thresholds or paid-exclusive PUBG API facts."
    - "D-55, D-56, D-57: Lab storage must be privacy-safe, entitlement-aware, auditable, and conservative with health/body data."
  artifacts:
    - path: "drizzle/0012_spray_lab_sessions.sql"
      provides: "Database migration for Lab sessions, events, benchmark snapshots, and validation links."
      contains: "spray_lab_sessions"
    - path: "src/actions/spray-lab.ts"
      provides: "Authenticated server actions for creating, updating, completing, and linking Lab sessions."
      contains: "createSprayLabSessionAction"
    - path: "src/lib/spray-lab-projection.ts"
      provides: "Free/Pro projection of Lab session depth and benchmark visibility."
      contains: "projectSprayLabForAccess"
  key_links:
    - from: "src/core/spray-lab-session.ts"
      to: "src/actions/spray-lab.ts"
      via: "server action persists reducer output and event audit"
      pattern: "reduceSprayLabSessionEvent"
    - from: "src/types/monetization.ts"
      to: "src/lib/product-entitlements.ts"
      via: "spray_lab.session_runner and spray_lab.benchmarks entitlement truth"
      pattern: "spray_lab.session_runner"
---

<objective>
Persistir o Spray Lab e ligar acesso Free/Pro de forma server-owned.

Saida esperada: sessoes, eventos, snapshots e validation links ficam gravados com privacidade minima, e a UI futura recebe uma projecao honesta do que mostrar para Free e Pro.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-pubg-spray-lab-session-runner-and-benchmark/09-CONTEXT.md
@.planning/phases/09-pubg-spray-lab-session-runner-and-benchmark/09-RESEARCH.md
@src/db/schema.ts
@src/actions/history.ts
@src/lib/product-entitlements.ts
@src/lib/premium-projection.ts
@src/types/monetization.ts
@src/core/spray-lab-session.ts
@src/core/spray-lab-scoring.ts
</context>

<threat_model>
Scope: schema, actions, entitlements e projecao.

Threats:
- HIGH: persistir video bruto, dados de dor/saude ou rotina fisica detalhada. Mitigacao: apenas eventos, reason codes e snapshots de evidencia.
- HIGH: cliente consegue promover Free para Pro ou marcar benchmark validado. Mitigacao: actions autenticadas e projecao server-owned.
- HIGH: sessao de outro usuario vira validation link. Mitigacao: todas as actions checam owner da analise base, sessao Lab e clip de validacao.
- MEDIUM: schema vira JSON opaco demais para auditoria. Mitigacao: IDs/status/tiers/context key em colunas, snapshots JSON apenas para payload versionado.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Criar schema e migracao Lab</name>
  <files>src/db/schema.ts, drizzle/0012_spray_lab_sessions.sql</files>
  <read_first>src/db/schema.ts, drizzle/0011_complete_training_protocols.sql</read_first>
  <action>
Adicionar tabelas:
- `sprayLabSessions`;
- `sprayLabSessionEvents`;
- `sprayLabBenchmarkSnapshots`;
- `sprayLabValidationLinks`.

Incluir FK para usuario, analise base, protocolo/revision quando aplicavel, status, ato/estado, lane id, context key, evidence level, fidelity tier, validation status, timestamps e JSON versionado para snapshots.
  </action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Migration `0012` e idempotente em relacao ao schema Drizzle.
    - Relations permitem buscar sessoes por usuario, analise base e validation link.
    - Nao ha coluna para video bruto ou perfil medico.
  </acceptance_criteria>
  <done>Banco conhece o Lab sem violar privacidade.</done>
</task>

<task type="auto">
  <name>Task 2: Criar actions autenticadas do Lab</name>
  <files>src/actions/spray-lab.ts, src/actions/spray-lab.test.ts</files>
  <read_first>src/actions/history.ts, src/actions/dashboard.ts, src/core/spray-lab-session.ts</read_first>
  <action>
Implementar actions:
- `createSprayLabSessionAction`;
- `getActiveSprayLabSessionAction`;
- `recordSprayLabSessionEventAction`;
- `completeSprayLabSessionAction`;
- `recordSprayLabBenchmarkSnapshotAction`;
- `createSprayLabValidationLinkAction`;
- `resolveSprayLabValidationTargetAction`.

Todas devem validar usuario, ownership, protocolo base, status atual e transicao permitida.
  </action>
  <verify>npx vitest run src/actions/spray-lab.test.ts</verify>
  <acceptance_criteria>
    - Criar sessao a partir de resultado/historico/dashboard preserva contexto do protocolo.
    - Eventos invalidos nao mudam estado.
    - Sessao finalizada nao pode receber evento de spray novo sem reabrir fluxo explicitamente.
    - Validation target de outro usuario e negado.
  </acceptance_criteria>
  <done>Servidor controla o ciclo de vida do Lab.</done>
</task>

<task type="auto">
  <name>Task 3: Ligar snapshots e links de validacao</name>
  <files>src/actions/spray-lab.ts, src/actions/spray-lab.test.ts</files>
  <read_first>src/core/spray-lab-scoring.ts, src/core/spray-lab-validation.ts, src/actions/history.ts</read_first>
  <action>
Ao concluir sessao, compor fidelity/index/benchmark snapshot. Ao gravar validacao, ligar clip novo ao contexto base e atualizar status:
- validacao_confirmada;
- sinal_promissor;
- sem_mudanca_clara;
- regressao_validada;
- nao_compativel;
- inconclusivo.
  </action>
  <verify>npx vitest run src/actions/spray-lab.test.ts src/core/spray-lab-scoring.test.ts</verify>
  <acceptance_criteria>
    - Snapshot com fidelity `practice_only` nao vira benchmark validado.
    - Clip com variaveis alteradas gera `nao_compativel` ou evidencia fraca.
    - Link de validacao preserva IDs de base, Lab e clip validado.
  </acceptance_criteria>
  <done>Dados do Lab conseguem voltar para validacao compativel.</done>
</task>

<task type="auto">
  <name>Task 4: Projetar Free/Pro para Spray Lab</name>
  <files>src/lib/product-entitlements.ts, src/lib/premium-projection.ts, src/lib/spray-lab-projection.ts, src/lib/spray-lab-projection.test.ts</files>
  <read_first>src/types/monetization.ts, src/lib/product-entitlements.ts, src/lib/premium-projection.ts</read_first>
  <action>
Adicionar concessao/projecao de:
- `spray_lab.session_runner`;
- `spray_lab.benchmarks`.

Criar helper que retorne visibilidade Free/Pro para sessao basica, lanes, auditoria, historico, indice validado, benchmark por contexto e comparacoes. Free deve ter caminho util, mas Pro preserva profundidade comercial.
  </action>
  <verify>npx vitest run src/lib/spray-lab-projection.test.ts</verify>
  <acceptance_criteria>
    - Free nunca recebe campos Pro por confiar no cliente.
    - Pro recebe benchmarks e audit drawers quando entitlement existe.
    - Copy nao vende dado PUBG API exclusivo como feature paga.
  </acceptance_criteria>
  <done>Produto sabe o que mostrar do Lab por acesso.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/actions/spray-lab.test.ts src/lib/spray-lab-projection.test.ts`
- `npm run typecheck`
- aplicar/verificar `drizzle/0012_spray_lab_sessions.sql` no banco alvo quando a execucao estiver pronta para fechar a fase.
</verification>

<success_criteria>
- Sessoes, eventos, benchmark snapshots e validation links persistem com ownership.
- Projecao Free/Pro e server-owned e testada.
- Nenhum dado sensivel fora do necessario e armazenado.
- Validation links conseguem conectar Lab, analise base e clip novo.
</success_criteria>

<output>
Persistencia e acesso do Spray Lab prontos para UI e continuidade.
</output>
