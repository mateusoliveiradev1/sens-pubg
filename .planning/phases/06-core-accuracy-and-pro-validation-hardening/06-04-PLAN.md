---
phase: 06-core-accuracy-and-pro-validation-hardening
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/types/engine.ts
  - src/core/analysis-decision.ts
  - src/core/analysis-decision.test.ts
  - src/core/measurement-truth.ts
  - src/core/measurement-truth.test.ts
  - src/core/diagnostic-engine.ts
  - src/core/diagnostic-engine.test.ts
  - src/core/sensitivity-engine.ts
  - src/core/sensitivity-engine.test.ts
  - src/core/coach-signal-extractor.ts
  - src/core/coach-signal-extractor.test.ts
  - src/core/coach-plan-builder.ts
  - src/core/coach-plan-builder.test.ts
  - src/core/precision-loop.ts
  - src/core/precision-loop.test.ts
  - src/actions/history.ts
  - src/actions/history.test.ts
  - src/app/history/analysis-result-hydration.ts
  - src/app/history/analysis-result-hydration.test.ts
  - src/app/analyze/results-dashboard-view-model.ts
  - src/app/analyze/results-dashboard-view-model.test.ts
autonomous: true
requirements: [PREC-01, PREC-02, PREC-03, PREC-04, COACH-01, COACH-02, COACH-03, COACH-04, COACH-05]
user_setup: []
must_haves:
  truths:
    - "D-14: Tracking changes must recalibrate metrics, diagnostics, sensitivity, coach, precision trends, confidence, goldens, and benchmark expectations."
    - "D-15, D-21, D-22: New results use the versioned contract and decision ladder while old history remains readable."
    - "D-16, D-17, D-18, D-19: Confidence remains conservative; insufficient confidence blocks strong decisions but keeps safe help."
    - "D-23: Only useful results count quota; blocked and inconclusive recapture do not count."
    - "D-27: No False Done applies if any downstream recalibration gate fails or cannot run."
  artifacts:
    - path: "src/core/measurement-truth.ts"
      provides: "Legacy action-state compatibility and mastery payload derived from Phase 6 decision levels."
      contains: "resolveMeasurementTruth"
    - path: "src/core/sensitivity-engine.ts"
      provides: "Sensitivity tier safety from decision-level permissions."
      contains: "generateSensitivityRecommendation"
    - path: "src/core/coach-plan-builder.ts"
      provides: "Coach tier safety from decision-level permissions."
      contains: "resolveCoachDecisionTier"
    - path: "src/actions/history.ts"
      provides: "Versioned save/enrichment path and non-billable invalid/inconclusive behavior."
      contains: "isNonBillableWeakCapture"
  key_links:
    - from: "src/core/analysis-decision.ts"
      to: "src/core/sensitivity-engine.ts"
      via: "decision permission matrix blocks apply_ready below strong_analysis"
      pattern: "countsAsUsefulAnalysis"
    - from: "src/core/analysis-decision.ts"
      to: "src/actions/history.ts"
      via: "decision level controls useful-result quota and trend eligibility"
      pattern: "AnalysisDecisionLevel"
---

<objective>
Recalibrate diagnostics, sensitivity, coach, precision trends, history, and quota around the Phase 6 decision ladder.

Purpose: make every downstream recommendation obey the same validity/confidence contract after spray-window and tracking hardening.
Output: decision-aware measurement truth, diagnostics, sensitivity, coach, precision compatibility, history hydration, quota behavior, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-CONTEXT.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-RESEARCH.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-VALIDATION.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-PATTERNS.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-01-SUMMARY.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-02-SUMMARY.md
@src/core/analysis-decision.ts
@src/core/measurement-truth.ts
@src/core/diagnostic-engine.ts
@src/core/sensitivity-engine.ts
@src/core/coach-plan-builder.ts
@src/core/precision-loop.ts
@src/actions/history.ts
</context>

<threat_model>
Scope: downstream recommendation safety, history compatibility, trend eligibility, and useful-result quota.

Threats:
- HIGH: downstream modules ignore the new decision ladder and still emit strong diagnosis, sensitivity, or coach actions from weak clips. Mitigation: decision-level checks are added to each tier resolver and tested directly.
- HIGH: old history cannot hydrate because new fields are assumed. Mitigation: all new result fields are optional and hydration defaults legacy results to the old truth contract.
- HIGH: invalid/inconclusive results consume paid quota. Mitigation: history save checks decision permissions before finalizing quota.
- MEDIUM: trend math mixes old/new engine contracts. Mitigation: precision compatibility includes engine version and decision level blockers.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make measurement truth and diagnostics decision-aware</name>
  <files>src/types/engine.ts, src/core/analysis-decision.ts, src/core/analysis-decision.test.ts, src/core/measurement-truth.ts, src/core/measurement-truth.test.ts, src/core/diagnostic-engine.ts, src/core/diagnostic-engine.test.ts</files>
  <read_first>src/types/engine.ts, src/core/analysis-decision.ts, src/core/measurement-truth.ts, src/core/measurement-truth.test.ts, src/core/diagnostic-engine.ts, src/core/diagnostic-engine.test.ts</read_first>
  <action>
Add optional `analysisDecision?: AnalysisDecision` to `AnalysisResult`.

Update `resolveMeasurementTruth`:
- prefer `analysisDecision.level` when present;
- map `blocked_invalid_clip` and `inconclusive_recapture` to legacy `capture_again`;
- map `partial_safe_read` to legacy `inconclusive`;
- require `strong_analysis` before returning legacy `ready`;
- append blocker reasons from `analysisDecision.blockerReasons` to `blockedRecommendations`.

Update `runDiagnostics`:
- accept optional decision context via function parameter or metrics/analysis payload pattern chosen by executor;
- return only an `inconclusive` diagnosis when decision level is `blocked_invalid_clip`, `inconclusive_recapture`, or `partial_safe_read`;
- keep existing diagnosis behavior for `usable_analysis` and `strong_analysis`.
  </action>
  <verify>npx vitest run src/core/analysis-decision.test.ts src/core/measurement-truth.test.ts src/core/diagnostic-engine.test.ts</verify>
  <acceptance_criteria>
    - `AnalysisResult` has optional `analysisDecision`.
    - Measurement truth tests cover all five decision levels.
    - Diagnostic tests prove blocked/recapture/partial decisions do not emit overpull, underpull, jitter, drift, timing, or inconsistency as primary diagnosis.
    - Legacy tests without `analysisDecision` still pass.
  </acceptance_criteria>
  <done>Mastery and diagnosis cannot speak stronger than the Phase 6 decision ladder allows.</done>
</task>

<task type="auto">
  <name>Task 2: Gate sensitivity and coach actions by decision permissions</name>
  <files>src/core/sensitivity-engine.ts, src/core/sensitivity-engine.test.ts, src/core/coach-signal-extractor.ts, src/core/coach-signal-extractor.test.ts, src/core/coach-plan-builder.ts, src/core/coach-plan-builder.test.ts</files>
  <read_first>src/core/sensitivity-engine.ts, src/core/sensitivity-engine.test.ts, src/core/coach-signal-extractor.ts, src/core/coach-plan-builder.ts, src/core/coach-plan-builder.test.ts, src/core/analysis-decision.ts, src/types/engine.ts</read_first>
  <action>
Update sensitivity generation and coach planning so decision level controls strong actions.

Sensitivity:
- below `usable_analysis`, return tier `capture_again`, evidenceTier `weak`, and reasoning containing `decisionLevel=<level>`.
- at `usable_analysis`, allow `test_profiles` but block `apply_ready`.
- require `strong_analysis`, strong evidence, and repeated compatible clips before `apply_ready`.

Coach:
- below `usable_analysis`, force `capture_again` or `test_protocol` depending on permission matrix, never `apply_protocol`.
- at `usable_analysis`, allow `test_protocol` or `stabilize_block`, never `apply_protocol`.
- require `strong_analysis` plus existing compatible validation before `apply_protocol`.

Signal extractor:
- add a decision-ladder signal key such as `analysis_decision.<level>` with confidence/coverage from the decision.
  </action>
  <verify>npx vitest run src/core/sensitivity-engine.test.ts src/core/coach-signal-extractor.test.ts src/core/coach-plan-builder.test.ts</verify>
  <acceptance_criteria>
    - Sensitivity tests prove `blocked_invalid_clip`, `inconclusive_recapture`, and `partial_safe_read` cannot produce `apply_ready`.
    - Coach tests prove `usable_analysis` cannot produce `apply_protocol`.
    - Coach tests prove `strong_analysis` still requires compatible validation before `apply_protocol`.
    - Reasoning or signal output contains `analysis_decision.`.
  </acceptance_criteria>
  <done>Coach and sensitivity cannot outrun the confidence and validity contract.</done>
</task>

<task type="auto">
  <name>Task 3: Version precision trends, history hydration, and useful-result quota</name>
  <files>src/core/precision-loop.ts, src/core/precision-loop.test.ts, src/actions/history.ts, src/actions/history.test.ts, src/app/history/analysis-result-hydration.ts, src/app/history/analysis-result-hydration.test.ts, src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard-view-model.test.ts</files>
  <read_first>src/core/precision-loop.ts, src/core/precision-loop.test.ts, src/actions/history.ts, src/actions/history.test.ts, src/app/history/analysis-result-hydration.ts, src/app/history/analysis-result-hydration.test.ts, src/app/analyze/results-dashboard-view-model.ts, src/types/engine.ts</read_first>
  <action>
Update trend, history, and quota behavior.

Precision loop:
- add `engine_version_mismatch` and `decision_level_insufficient` compatibility blockers;
- require `analysisDecision.version === 'spray-truth-v2'` for new Phase 6 trend math;
- block trend math below `usable_analysis`.

History hydration:
- hydrate old results without `analysisDecision` as legacy results;
- preserve old `mastery`, `precisionTrend`, `coachPlan`, and `sensitivity` fields.

Save/quota:
- update `isNonBillableWeakCapture` or equivalent to return true when `analysisDecision.permissionMatrix.countsAsUsefulAnalysis === false`;
- final quota should void or mark non-billable for `blocked_invalid_clip`, `inconclusive_recapture`, and `partial_safe_read`;
- saved audit result should keep `analysisDecision` and recapture guidance.

Results view model:
- expose decision-level blocked reasons in existing verdict/quota copy without adding a visual redesign.
  </action>
  <verify>npx vitest run src/core/precision-loop.test.ts src/actions/history.test.ts src/app/history/analysis-result-hydration.test.ts src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - Precision tests cover `engine_version_mismatch` and `decision_level_insufficient`.
    - History hydration tests pass for old results without `analysisDecision`.
    - History action tests prove non-useful decision levels do not consume quota.
    - View-model tests show blocker reasons while avoiding perfect/guaranteed copy.
  </acceptance_criteria>
  <done>Saved results, quota, and trends all respect the same versioned decision permissions.</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/core/analysis-decision.test.ts src/core/measurement-truth.test.ts src/core/diagnostic-engine.test.ts src/core/sensitivity-engine.test.ts src/core/coach-signal-extractor.test.ts src/core/coach-plan-builder.test.ts src/core/precision-loop.test.ts src/actions/history.test.ts src/app/history/analysis-result-hydration.test.ts src/app/analyze/results-dashboard-view-model.test.ts
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- Measurement truth, diagnostics, sensitivity, coach, trends, history, and quota consume the versioned decision ladder.
- `apply_ready` and `apply_protocol` require `strong_analysis` plus existing evidence/history gates.
- Invalid/inconclusive/partial reads are non-billable useful-result saves.
- Old history remains readable.
- Trend math cannot silently mix legacy and Phase 6 truth contracts.

</success_criteria>

<output>
After completion, create `.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-04-SUMMARY.md` with downstream recalibration evidence.
</output>
