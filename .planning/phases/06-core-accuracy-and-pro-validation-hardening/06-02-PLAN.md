---
phase: 06-core-accuracy-and-pro-validation-hardening
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/types/engine.ts
  - src/core/global-motion-compensation.ts
  - src/core/global-motion-compensation.test.ts
  - src/core/crosshair-tracking.ts
  - src/core/crosshair-tracking.test.ts
  - src/core/tracking-evidence.ts
  - src/core/tracking-evidence.test.ts
  - src/core/spray-metrics.ts
  - src/core/spray-metrics.test.ts
  - src/workers/aim-analyzer-session.ts
  - src/app/analyze/tracking-result-mapper.ts
  - src/app/analyze/tracking-result-mapper.test.ts
  - scripts/run-benchmark.ts
  - tests/goldens/benchmark/synthetic-benchmark.v1.json
autonomous: true
requirements: [PREC-01, PREC-02, PREC-04, BENCH-01, BENCH-03]
user_setup: []
must_haves:
  truths:
    - "D-12, D-13: Wave 2 hardens tracking/recoil with camera separation; flicks, cuts, swaps, camera shake, and external movement must not become player recoil error."
    - "D-14: Tracking changes must recalibrate spray metrics, diagnostics, sensitivity, coach, precision trends, confidence, goldens, and benchmark expectations."
    - "D-16, D-18: Stronger tone requires valid spray, high coverage/confidence, clean tracking, metadata, and no critical blocker."
    - "D-26: Tracking, diagnostic, coach, captured, and benchmark gates must catch commercial regressions."
  artifacts:
    - path: "src/core/crosshair-tracking.ts"
      provides: "Reticle tracking with explicit exogenous motion, hard-cut, flick, target-swap, and reacquisition evidence."
      contains: "createStreamingCrosshairTracker"
    - path: "src/core/tracking-evidence.ts"
      provides: "Tracking evidence summary used by metrics and calibration."
      contains: "createTrackingEvidence"
    - path: "src/core/spray-metrics.ts"
      provides: "Metric-quality penalties and residual filtering that exclude exogenous motion from player recoil."
      contains: "buildEvidenceQualityFromFrames"
  key_links:
    - from: "src/core/global-motion-compensation.ts"
      to: "src/core/crosshair-tracking.ts"
      via: "motion transition classification informs tracker disturbance and fallback confidence"
      pattern: "GlobalMotionEstimate"
    - from: "src/core/crosshair-tracking.ts"
      to: "src/core/spray-metrics.ts"
      via: "tracking frame disturbance fields reduce confidence and can exclude contaminated residuals"
      pattern: "exogenousDisturbance"
---

<objective>
Harden reticle tracking and recoil evidence so exogenous motion does not become player recoil error.

Purpose: improve camera-motion separation, hard-cut/flick/target-swap handling, reacquisition confidence, metric quality, and benchmark visibility while preserving browser-first worker analysis.
Output: richer tracking observations, global motion transition classification, disturbance-aware metrics, worker mapping updates, and benchmark tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-CONTEXT.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-RESEARCH.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-VALIDATION.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-PATTERNS.md
@.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-01-SUMMARY.md
@src/core/global-motion-compensation.ts
@src/core/crosshair-tracking.ts
@src/core/tracking-evidence.ts
@src/core/spray-metrics.ts
@src/workers/aim-analyzer-session.ts
@src/app/analyze/tracking-result-mapper.ts
</context>

<threat_model>
Scope: frame tracking, global motion, worker mapping, metric evidence, and benchmark assertions.

Threats:
- HIGH: camera shake, hard cuts, flicks, and target swaps are scored as recoil mistakes. Mitigation: classify motion transitions and mark contaminated frames before residuals are calculated.
- HIGH: reacquisition after decoy drift appears confident even when target identity is uncertain. Mitigation: add reacquisition/identity penalties to tracking evidence and metric quality.
- MEDIUM: worker and engine types drift. Mitigation: mapping tests assert every new observation field survives worker-to-engine conversion.
- MEDIUM: benchmark expectations do not notice cleaner but stricter tracking. Mitigation: update synthetic benchmark expectations and release report fields.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add global motion transition classification</name>
  <files>src/types/engine.ts, src/core/global-motion-compensation.ts, src/core/global-motion-compensation.test.ts</files>
  <read_first>src/types/engine.ts, src/core/global-motion-compensation.ts, src/core/global-motion-compensation.test.ts, src/core/crosshair-tracking.ts</read_first>
  <action>
Extend `GlobalMotionEstimate` with optional fields:
- `magnitudePx`
- `transitionKind: 'stable' | 'camera_motion' | 'hard_cut'`
- `reliability: number`

Add `classifyGlobalMotionTransition(previousFrame, currentFrame, options)` that wraps `estimateGlobalMotion` and returns the extended estimate.

Concrete classification rules:
- `stable` when magnitude is below 2 px or confidence below 0.2.
- `camera_motion` when magnitude is 2-24 px and confidence is at least 0.35.
- `hard_cut` when mean absolute error is above 72 or magnitude is above 24 px with confidence below 0.35.

Keep existing `estimateGlobalMotion` return fields backward-compatible.
  </action>
  <verify>npx vitest run src/core/global-motion-compensation.test.ts</verify>
  <acceptance_criteria>
    - `GlobalMotionEstimate` includes `transitionKind`.
    - Tests cover `stable`, `camera_motion`, and `hard_cut`.
    - Existing global motion shift test still passes.
    - No caller is required to pass browser or DOM dependencies.
  </acceptance_criteria>
  <done>The tracker can distinguish stable frames, camera motion, and hard cuts before scoring recoil.</done>
</task>

<task type="auto">
  <name>Task 2: Extend streaming tracker disturbance and identity evidence</name>
  <files>src/types/engine.ts, src/core/crosshair-tracking.ts, src/core/crosshair-tracking.test.ts, src/workers/aim-analyzer-session.ts, src/app/analyze/tracking-result-mapper.ts, src/app/analyze/tracking-result-mapper.test.ts</files>
  <read_first>src/types/engine.ts, src/core/crosshair-tracking.ts, src/core/crosshair-tracking.test.ts, src/workers/aim-analyzer-session.ts, src/app/analyze/tracking-result-mapper.ts, src/app/analyze/tracking-result-mapper.test.ts, src/core/global-motion-compensation.ts</read_first>
  <action>
Extend `ReticleExogenousDisturbance` or add adjacent observation fields with:
- `cameraMotion`
- `hardCut`
- `flick`
- `targetSwap`
- `identityConfidence`

Update `createStreamingCrosshairTracker`:
- use `classifyGlobalMotionTransition` when global motion compensation is enabled;
- mark hard cuts as `status='lost'` or `status='occluded'` with `hardCut > 0.8`;
- mark camera motion separately from reticle movement;
- detect flick/target swap when visible reticle displacement exceeds 28 px in one frame or exceeds 40 px in two nearby jumps;
- cap confidence at 0.49 for target-swap/flick contaminated observations;
- preserve the current good reticle, neutral reticle, template fallback, muzzle flash, shake, and reacquisition tests.

Update worker result and mapper types so all new fields survive from worker output into engine `TrackingFrameObservation`.
  </action>
  <verify>npx vitest run src/core/crosshair-tracking.test.ts src/app/analyze/tracking-result-mapper.test.ts</verify>
  <acceptance_criteria>
    - Tracker tests prove camera motion increases `cameraMotion` without increasing player recoil confidence.
    - Tracker tests prove hard cuts produce lost/occluded frames with `hardCut > 0.8`.
    - Tracker tests prove flick/target swap cap confidence below 0.5.
    - Mapper tests prove `cameraMotion`, `hardCut`, `flick`, `targetSwap`, and `identityConfidence` are preserved.
  </acceptance_criteria>
  <done>Frame observations can explain exogenous contamination instead of hiding it in a generic confidence score.</done>
</task>

<task type="auto">
  <name>Task 3: Make tracking evidence and spray metrics disturbance-aware</name>
  <files>src/core/tracking-evidence.ts, src/core/tracking-evidence.test.ts, src/core/spray-metrics.ts, src/core/spray-metrics.test.ts</files>
  <read_first>src/core/tracking-evidence.ts, src/core/tracking-evidence.test.ts, src/core/spray-metrics.ts, src/core/spray-metrics.test.ts, src/types/engine.ts</read_first>
  <action>
Extend tracking evidence and metric quality with contamination-aware summaries.

Add to `TrackingEvidence` or `MetricEvidenceQuality`:
- `cameraMotionPenalty`
- `hardCutPenalty`
- `flickPenalty`
- `targetSwapPenalty`
- `identityPenalty`
- `contaminatedFrameCount`

Update `createTrackingEvidence` and `buildEvidenceQualityFromFrames` so:
- camera motion reduces confidence and coverage but does not increase recoil error;
- hard-cut, flick, and target-swap frames are excluded from residual sample quality when possible;
- contaminated frame count appears in evidence summaries;
- confidence cannot be `strong_analysis` eligible when `hardCutPenalty`, `flickPenalty`, or `targetSwapPenalty` is above 0.25.
  </action>
  <verify>npx vitest run src/core/tracking-evidence.test.ts src/core/spray-metrics.test.ts</verify>
  <acceptance_criteria>
    - Tracking evidence tests contain `contaminatedFrameCount`.
    - Spray metrics tests prove camera-motion frames reduce evidence confidence without increasing vertical-control residuals.
    - Spray metrics tests prove hard-cut/flick/target-swap frames lower `metricQuality.sprayScore.confidence`.
    - Existing spray metrics tests still pass.
  </acceptance_criteria>
  <done>Metric confidence reflects clean tracking evidence instead of treating contaminated frames as player mechanics.</done>
</task>

<task type="auto">
  <name>Task 4: Add benchmark expectations for tracking contamination</name>
  <files>src/types/benchmark.ts, src/types/benchmark.test.ts, scripts/run-benchmark.ts, src/core/benchmark-runner.test.ts, tests/goldens/benchmark/synthetic-benchmark.v1.json</files>
  <read_first>src/types/benchmark.ts, src/types/benchmark.test.ts, scripts/run-benchmark.ts, src/core/benchmark-runner.test.ts, tests/goldens/benchmark/synthetic-benchmark.v1.json, src/core/tracking-evidence.ts</read_first>
  <action>
Extend benchmark expectations with optional tracking contamination fields:
- `expectedTracking.contaminatedFrameCountMax`
- `expectedTracking.cameraMotionPenaltyMax`
- `expectedTracking.hardCutPenaltyMax`
- `expectedTracking.flickPenaltyMax`
- `expectedTracking.targetSwapPenaltyMax`

Update `scripts/run-benchmark.ts` to compare these fields when present and report mismatches with clip ID, field name, expected max, and actual value.

Add at least one synthetic benchmark case that expects contaminated tracking to block or downgrade strong output.
  </action>
  <verify>npx vitest run src/types/benchmark.test.ts src/core/benchmark-runner.test.ts && npm run benchmark:gate</verify>
  <acceptance_criteria>
    - Benchmark schema accepts the five optional contamination expectation fields.
    - Runner reports a mismatch when actual contamination exceeds expected max.
    - `npm run benchmark:gate` runs at least one contamination-aware expectation.
    - `benchmark:gate` remains the fast daily command in `package.json`.
  </acceptance_criteria>
  <done>Tracking hardening is protected by benchmark fixtures, not only unit tests.</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/core/global-motion-compensation.test.ts src/core/crosshair-tracking.test.ts src/core/tracking-evidence.test.ts src/core/spray-metrics.test.ts src/app/analyze/tracking-result-mapper.test.ts src/types/benchmark.test.ts src/core/benchmark-runner.test.ts
- [ ] npm run benchmark:gate
- [ ] npm run typecheck
- [ ] npx vitest run
</verification>

<success_criteria>

- Camera motion, hard cuts, flicks, and target swaps are explicit tracking evidence.
- Contaminated frames reduce confidence/coverage and do not inflate recoil error.
- Worker-to-engine mapping preserves new tracking fields.
- Benchmark fixtures can fail on tracking contamination regressions.
- Browser-first tracking remains intact.

</success_criteria>

<output>
After completion, create `.planning/phases/06-core-accuracy-and-pro-validation-hardening/06-02-SUMMARY.md` with tracking, benchmark, and evidence changes.
</output>
