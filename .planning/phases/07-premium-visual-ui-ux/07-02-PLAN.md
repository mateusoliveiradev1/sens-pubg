---
phase: 07-premium-visual-ui-ux
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/ui/components/weapon-icon.tsx
  - src/ui/components/weapon-icon.contract.test.tsx
  - src/ui/components/weapon-icon.module.css
  - src/ui/components/weapon-visual-registry.ts
  - src/ui/components/weapon-visual-registry.test.ts
  - src/ui/components/weapon-support-status.ts
  - src/ui/components/weapon-support-status.test.ts
  - src/db/weapon-profile-seed.ts
  - src/app/analyze/analysis-weapon-support.ts
  - src/app/analyze/analysis-weapon-support.test.ts
  - src/app/dashboard/page.contract.test.ts
  - src/app/history/page.contract.test.ts
  - e2e/phase7.weapon-icons.spec.ts
autonomous: true
requirements: [MON-01, MON-02, MON-04]
user_setup: []
must_haves:
  truths:
    - "D-31, D-32, D-33, D-34, D-35, D-36: Deliver original authored SVG silhouettes for all 29 seed weapons, never use official/extracted assets, never generic-fallback existing seed weapons, and keep technical support limits honest."
    - "D-22: Weapon and brand art must remain safe from official PUBG/KRAFTON asset misuse or affiliation implication."
    - "D-47: SVG and icon rendering must stay lightweight, stable, responsive, and mobile-safe."
    - "D-55, D-58: Weapon grids/lists need responsive structure, accessible labels, focus states, contrast, and no overflow."
    - "D-67: Verification must include contract tests and visual render grid screenshots for all 29 weapons."
  artifacts:
    - path: "src/ui/components/weapon-visual-registry.ts"
      provides: "Canonical visual registry for all 29 seed weapons with silhouette ids and non-generic status."
      contains: "weaponVisualRegistry"
    - path: "src/ui/components/weapon-icon.tsx"
      provides: "Premium authored SVG rendering for all current seed weapons and status-aware fallback for future/non-seed weapons."
      contains: "WeaponIcon"
    - path: "src/ui/components/weapon-support-status.ts"
      provides: "Visual/technical support status helper so UI can show catalog truth without claiming all 29 are fully calibrated."
      contains: "resolveWeaponSupportStatus"
  key_links:
    - from: "src/db/weapon-profile-seed.ts"
      to: "src/ui/components/weapon-visual-registry.ts"
      via: "visual registry must cover every seed weapon name"
      pattern: "weaponSeeds"
    - from: "src/game/pubg/weapon-data.ts"
      to: "src/ui/components/weapon-support-status.ts"
      via: "technical registry determines support status separately from visual coverage"
      pattern: "getWeapon"
---

<objective>
Build the 29-weapon authored SVG catalog and support-status contract.

Purpose: make the visual weapon catalog feel premium and complete while staying honest that visual support and technical analysis calibration are separate truths.
Output: registry, premium silhouettes, accessible WeaponIcon rendering, support-status helper, contract tests, and Playwright visual grid coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-premium-visual-ui-ux/07-CONTEXT.md
@.planning/phases/07-premium-visual-ui-ux/07-UI-SPEC.md
@.planning/phases/07-premium-visual-ui-ux/07-RESEARCH.md
@.planning/phases/07-premium-visual-ui-ux/07-01-SUMMARY.md
@src/ui/components/weapon-icon.tsx
@src/db/weapon-profile-seed.ts
@src/game/pubg/weapon-data.ts
@src/game/pubg/weapon-patch-catalog.ts
@src/app/analyze/analysis-weapon-support.ts
@src/app/analyze/analysis-weapon-support.test.ts
@src/app/dashboard/page.contract.test.ts
@src/app/history/page.contract.test.ts
</context>

<threat_model>
Scope: weapon visual registry, authored SVG silhouettes, support labels, and visual verification.

Threats:
- HIGH: Existing seed weapons fall back to generic AR/SMG/DMR silhouettes. Mitigation: registry/test asserts every seed name resolves to a non-generic silhouette id.
- HIGH: UI implies all 29 weapons have full technical/calibration support. Mitigation: support-status helper distinguishes `suporte completo`, `suporte visual`, and `suporte tecnico limitado`.
- HIGH: SVGs trace official PUBG images or extracted assets. Mitigation: all geometry is authored from simple primitives/proportions and no asset imports are allowed.
- MEDIUM: Icons overflow or lose identity at mobile/nav sizes. Mitigation: shared viewBox/grid, stable aspect ratio, visual render grid, accessible labels.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the visual registry and support status model</name>
  <files>src/ui/components/weapon-visual-registry.ts, src/ui/components/weapon-visual-registry.test.ts, src/ui/components/weapon-support-status.ts, src/ui/components/weapon-support-status.test.ts, src/app/analyze/analysis-weapon-support.ts, src/app/analyze/analysis-weapon-support.test.ts</files>
  <read_first>src/db/weapon-profile-seed.ts, src/game/pubg/weapon-data.ts, src/app/analyze/analysis-weapon-support.ts, .planning/phases/07-premium-visual-ui-ux/07-UI-SPEC.md</read_first>
  <action>
Add a visual registry that maps every current `weaponSeeds` entry to:
- stable slug/id;
- display name;
- category;
- `silhouetteId`;
- visual support state;
- whether the current technical registry can analyze it.

Add `resolveWeaponSupportStatus(input)` with labels:
- `suporte completo` for seed weapons with technical registry support;
- `suporte visual` for seed weapons with authored icon but no full technical analysis registry entry;
- `suporte tecnico limitado` when analysis exists but calibration caveats apply;
- `removida` / `deprecated` for explicit future statuses only.

Do not mutate engine weapon data to pretend all 29 have full technical support.
  </action>
  <verify>npx vitest run src/ui/components/weapon-visual-registry.test.ts src/ui/components/weapon-support-status.test.ts src/app/analyze/analysis-weapon-support.test.ts</verify>
  <acceptance_criteria>
    - Tests prove every `weaponSeeds` name has a visual registry entry.
    - Tests prove QBZ/K2/Groza/FAMAS/M16A4/Mk47/JS9/P90/Mk12/Dragunov/QBU/VSS/Mk14 do not require technical registry support to render visually.
    - Existing analysis support tests still prove unsupported database-only entries are not selected for technical analysis.
  </acceptance_criteria>
  <done>Visual catalog coverage is complete without weakening technical truth.</done>
</task>

<task type="auto">
  <name>Task 2: Expand WeaponIcon to all 29 authored silhouettes</name>
  <files>src/ui/components/weapon-icon.tsx, src/ui/components/weapon-icon.module.css, src/ui/components/weapon-icon.contract.test.tsx, src/ui/components/weapon-visual-registry.ts</files>
  <read_first>src/ui/components/weapon-icon.tsx, src/ui/components/premium-ui.module.css, src/ui/components/weapon-visual-registry.ts, .planning/phases/07-premium-visual-ui-ux/07-UI-SPEC.md</read_first>
  <action>
Refactor `WeaponIcon` around the visual registry:
- one shared `viewBox` and visual grid for all silhouettes;
- authored shape functions for the 29 seed weapons;
- no existing seed weapon may render a generic category fallback;
- non-seed/future ids may render a premium fallback plus status label;
- preserve accessible label and `role="img"` behavior;
- expose stable size/framed/status props for dashboard, history, landing, pricing, and visual grid contexts;
- move repeated frame/status styling out of inline style where practical into `weapon-icon.module.css`.

Keep geometry original and simple: distinctive stocks, receivers, mags, rails, barrels, suppressor/integrated profiles, bullpup forms, DMR length, and category proportions are enough. Do not import images.
  </action>
  <verify>npx vitest run src/ui/components/weapon-icon.contract.test.tsx src/ui/components/weapon-visual-registry.test.ts</verify>
  <acceptance_criteria>
    - Contract tests assert all 29 seed weapons render a named non-generic silhouette.
    - Contract tests assert accessible labels include weapon display names.
    - Contract tests reject generic fallback for any current seed name.
    - No official/external asset imports are added.
  </acceptance_criteria>
  <done>The weapon catalog looks authored and complete for current seed weapons.</done>
</task>

<task type="auto">
  <name>Task 3: Add visual grid verification and route consumers</name>
  <files>e2e/phase7.weapon-icons.spec.ts, src/app/dashboard/page.contract.test.ts, src/app/history/page.contract.test.ts, src/ui/components/weapon-icon.contract.test.tsx</files>
  <read_first>e2e/pages.spec.ts, e2e/responsive.spec.ts, src/app/dashboard/page.tsx, src/app/history/page.tsx, src/ui/components/weapon-icon.tsx</read_first>
  <action>
Add verification coverage:
- create a Playwright visual check that renders or reaches a 29-weapon grid at mobile and desktop sizes. Prefer a test-only/dev-only route or a route section already showing the full catalog; do not expose a confusing production feature page.
- screenshot the 29-weapon grid and assert visible names/status labels where accessible.
- update dashboard/history contract tests if necessary so they keep using `WeaponIcon` and not emoji placeholders.
- ensure route consumers can pass either weapon id or weapon name and receive the correct seed visual.
  </action>
  <verify>npx vitest run src/ui/components/weapon-icon.contract.test.tsx src/app/dashboard/page.contract.test.ts src/app/history/page.contract.test.ts && npx playwright test e2e/phase7.weapon-icons.spec.ts</verify>
  <acceptance_criteria>
    - Playwright screenshot covers all 29 weapons at desktop and mobile sizes.
    - Seed weapons never show generic fallback in the grid.
    - Labels and status copy are readable and do not overflow at mobile width.
  </acceptance_criteria>
  <done>Weapon assets have both contract and visual evidence before route-level polish depends on them.</done>
</task>

</tasks>

<verification>
- [ ] npx vitest run src/ui/components/weapon-visual-registry.test.ts src/ui/components/weapon-support-status.test.ts src/ui/components/weapon-icon.contract.test.tsx src/app/analyze/analysis-weapon-support.test.ts src/app/dashboard/page.contract.test.ts src/app/history/page.contract.test.ts
- [ ] npx playwright test e2e/phase7.weapon-icons.spec.ts
- [ ] npm run typecheck
</verification>

<success_criteria>

- All 29 current seed weapons have authored, non-generic SVG silhouettes.
- UI differentiates visual support from full technical analysis support.
- Existing technically unsupported seed weapons still cannot be analyzed as if fully calibrated.
- Weapon icons have accessible labels, stable sizing, dark-surface contrast, and mobile/desktop visual evidence.
- No official PUBG/KRAFTON images, logos, extracted assets, or affiliation signals are introduced.

</success_criteria>

<output>
After completion, create `.planning/phases/07-premium-visual-ui-ux/07-02-SUMMARY.md` with the 29-weapon registry table, support-status evidence, screenshot paths, and test results.
</output>
