---
phase: 04-adaptive-coach-loop
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - src/core/coach-llm-contract.ts
  - src/core/coach-llm-adapter.ts
  - src/core/coach-llm-adapter.test.ts
  - src/core/analysis-result-coach-enrichment.ts
  - src/core/analysis-result-coach-enrichment.test.ts
  - src/types/benchmark.ts
  - scripts/run-benchmark.ts
  - tests/goldens/benchmark/synthetic-benchmark.v1.json
  - src/app/copy-claims.contract.test.ts
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03, COACH-04, COACH-05]
user_setup: []
must_haves:
  truths:
    - "D-27: apply_protocol requires strong current evidence, compatible memory without conflict, and recent validation."
    - "D-28: stabilize_block is used when useful signal lacks enough variance/history/trend support for stronger action."
    - "D-29: Sensitivity/technique conflict chooses least irreversible safe path."
    - "D-30: Weak evidence downgrades aggressive action to a test protocol with explanation."
    - "D-31: The coach must not repeat the same protocol blindly."
    - "D-33: Conflict between human outcome and compatible clip blocks apply_protocol."
    - "D-36: Aligned evidence can sound firmer but still includes validation."
    - "D-40: LLM rewrite is copy-only and cannot change tier, priority, order, scores, thresholds, validation, attachments, status, blocker reasons, outcome facts, or technical payloads."
    - "D-41: Coach copy is direct, demanding, premium, and honest with no perfection, rank, or guaranteed improvement claims."
    - "D-42: Phase 4 needs targeted tests and golden/benchmark checks for tier, primary focus, next block, outcome handling, conflict, LLM preservation, and memory."
  artifacts:
    - path: "src/core/coach-llm-contract.ts"
      provides: "Strict copy-only prompt/input schema including immutable outcome and memory facts."
      contains: "buildCoachInput"
    - path: "src/core/coach-llm-adapter.ts"
      provides: "ID-matched plan copy application that rejects outcome/status/blocker fact drift."
      contains: "adaptCoachResultWithOptionalLlm"
    - path: "src/types/benchmark.ts"
      provides: "Benchmark expectations for adaptive coach tier, focus, protocol, outcome memory, and conflict behavior."
      contains: "expectedAdaptiveCoach"
    - path: "scripts/run-benchmark.ts"
      provides: "Benchmark gate assertions for adaptive coach truth and LLM-safe deterministic facts."
      contains: "compareTruthExpectation"
  key_links:
    - from: "src/core/coach-llm-contract.ts"
      to: "src/core/coach-llm-adapter.ts"
      via: "schema limits LLM output to copy fields and adapter applies by exact IDs only"
      pattern: "CoachPlanTextOutputSchema"
    - from: "scripts/run-benchmark.ts"
      to: "src/core/coach-plan-builder.ts"
      via: "benchmark derives deterministic adaptive coach expectations from local core behavior"
      pattern: "buildCoachPlan"
---

<objective>
Harden the adaptive coach loop with LLM fact preservation and benchmark coverage.

Purpose: make sure optional LLM rewrite cannot alter technical truth, and make `benchmark:gate` catch regressions in coach tier, priority, next-block protocol, outcome memory, and conflict behavior.
Output: stricter LLM contract/adapter tests, benchmark schema/runner expectations, fixture updates, copy-claim tests, and full phase gates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adaptive-coach-loop/04-CONTEXT.md
@.planning/phases/04-adaptive-coach-loop/04-RESEARCH.md
@.planning/phases/04-adaptive-coach-loop/04-VALIDATION.md
@.planning/phases/04-adaptive-coach-loop/04-02-SUMMARY.md
@.planning/phases/04-adaptive-coach-loop/04-03-SUMMARY.md
@src/core/coach-llm-contract.ts
@src/core/coach-llm-adapter.ts
@src/core/analysis-result-coach-enrichment.ts
@src/types/benchmark.ts
@scripts/run-benchmark.ts
@tests/goldens/benchmark/synthetic-benchmark.v1.json
</context>

<threat_model>
Scope: optional LLM rewrite and deterministic benchmark safety net.

Threats:
- HIGH: LLM changes tier, focus order, protocol ids, thresholds, blockers, outcome status, or conflict facts. Mitigation: strict output schema, exact expected keys, id-matched application, immutable technical fields, deterministic fallback on any drift.
- MEDIUM: benchmark fixtures validate pleasant copy but miss adaptive coach truth. Mitigation: add schema/runner expectations for outcome memory, conflict, tier, primary focus, next block, and validation target.
- MEDIUM: copy claims imply perfect sensitivity or guaranteed improvement. Mitigation: copy-claims tests include new adaptive coach UI and LLM output fixtures.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend LLM contract with immutable outcome and memory facts</name>
  <files>src/core/coach-llm-contract.ts, src/core/coach-llm-adapter.ts, src/core/coach-llm-adapter.test.ts, src/core/analysis-result-coach-enrichment.ts, src/core/analysis-result-coach-enrichment.test.ts</files>
  <read_first>src/core/coach-llm-contract.ts, src/core/coach-llm-adapter.ts, src/core/coach-llm-adapter.test.ts, src/core/analysis-result-coach-enrichment.ts, src/core/analysis-result-coach-enrichment.test.ts, src/types/engine.ts, src/core/coach-outcomes.ts</read_first>
  <action>
Keep LLM rewrite copy-only as the coach payload grows.

Implementation guidance:
- Include deterministic outcome/memory facts in the LLM input for context only: tier, focus ids/order, protocol ids/order, next-block checks, blocker reasons, outcome status, reason codes, conflict state, precision trend label, thresholds, duration, and validation target.
- Keep output schema limited to currently allowed copy fields or a deliberately tiny new copy-only field list.
- Reject unknown keys, duplicate protocol ids, missing ids, reordered/mismatched protocols, attempts to add outcomes, attempts to alter statuses, and blocked English/overclaim copy.
- Adapter must return deterministic output when parsed output is invalid.
- Tests must simulate malicious or accidental rewrite attempts that change tier, outcome status, reason code, blocker reason, threshold, protocol id, focus order, and next-block duration.
  </action>
  <verify>npx vitest run src/core/coach-llm-adapter.test.ts src/core/analysis-result-coach-enrichment.test.ts</verify>
  <acceptance_criteria>
    - LLM input contains outcome/memory facts only as immutable context.
    - Output schema does not allow tier, status, reason code, blockers, thresholds, attachments, or scores.
    - Invalid LLM output falls back to deterministic coach plan.
    - Tests prove protocol id/order drift and outcome fact drift are rejected.
  </acceptance_criteria>
  <done>Optional LLM polish cannot mutate adaptive coach truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add adaptive coach benchmark expectations</name>
  <files>src/types/benchmark.ts, scripts/run-benchmark.ts, tests/goldens/benchmark/synthetic-benchmark.v1.json</files>
  <read_first>src/types/benchmark.ts, scripts/run-benchmark.ts, tests/goldens/benchmark/synthetic-benchmark.v1.json, src/core/coach-plan-builder.ts, src/core/coach-memory.ts</read_first>
  <action>
Extend the benchmark gate so Phase 4 coach behavior is not protected only by unit tests.

Recommended schema:
- Add optional `expectedAdaptiveCoach` under benchmark labels or `expectedTruth` with:
  - `tier`
  - `primaryFocusArea`
  - `secondaryFocusAreas`
  - `protocolId`
  - `outcomeMemoryState` such as `none`, `pending`, `weak_self_report`, `confirmed_progress`, `conflict`, `invalid_capture`, `repeated_failure`
  - `requiresCompatibleValidation`
  - `blocksApplyProtocol`
  - `nextBlockKey` or stable title/target marker

Runner behavior:
- Compare adaptive fields when present.
- Keep existing benchmark output stable for fixtures without adaptive expectations.
- Add or update synthetic benchmark cases for:
  - single strong clip cannot apply without memory;
  - improved self-report stays weak until compatible validation;
  - conflict blocks apply_protocol;
  - invalid capture does not count against protocol;
  - confirmed compatible progress consolidates.

Do not add a separate benchmark runner; extend `scripts/run-benchmark.ts`.
  </action>
  <verify>npm run benchmark:gate</verify>
  <acceptance_criteria>
    - Benchmark schema accepts adaptive coach expectations.
    - Runner reports mismatches for tier, primary focus, protocol id, validation requirement, and apply block.
    - Existing benchmark fixtures without adaptive expectations still parse.
    - `npm run benchmark:gate` exercises at least one adaptive coach expectation.
  </acceptance_criteria>
  <done>Release benchmark protects the adaptive coach contract, not just generic coach copy.</done>
</task>

<task type="auto">
  <name>Task 3: Run final copy and phase gates</name>
  <files>src/app/copy-claims.contract.test.ts, src/core/coach-llm-adapter.test.ts, tests/goldens/benchmark/synthetic-benchmark.v1.json</files>
  <read_first>src/app/copy-claims.contract.test.ts, .planning/phases/04-adaptive-coach-loop/04-UI-SPEC.md, src/core/coach-llm-adapter.test.ts, src/types/benchmark.ts</read_first>
  <action>
Extend copy-claim coverage and run the full phase validation gates.

Copy guardrails:
- Include new adaptive coach UI strings and any LLM fallback strings in `src/app/copy-claims.contract.test.ts`.
- Block perfect sensitivity, guaranteed improvement, rank-gain promises, definitive final verdicts, and LLM wording that implies the coach proved improvement without compatible validation.
- Ensure conflict copy stays honest and routes to validation.

Run final commands and fix regressions introduced by Phase 4 work:
- `npm run typecheck`
- `npx vitest run`
- `npm run benchmark:gate`
  </action>
  <verify>npm run typecheck && npx vitest run && npm run benchmark:gate</verify>
  <acceptance_criteria>
    - Copy claims test covers new adaptive coach surfaces.
    - Typecheck passes.
    - Full Vitest suite passes.
    - Benchmark gate passes with adaptive expectations.
  </acceptance_criteria>
  <done>The entire Phase 4 coach loop is guarded by deterministic tests and benchmark gates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/core/coach-llm-adapter.test.ts src/core/analysis-result-coach-enrichment.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- LLM rewrite remains schema-bound and copy-only.
- Technical facts, outcome facts, focus order, protocol order, blockers, thresholds, and validation facts cannot drift through LLM output.
- Benchmark gate covers adaptive coach memory and conflict behavior.
- Phase 4 UI and LLM copy remain honest and non-guaranteeing.
- Full validation defaults are ready for execute-phase.

</success_criteria>

<output>
After completion, create `.planning/phases/04-adaptive-coach-loop/04-04-SUMMARY.md`
</output>
