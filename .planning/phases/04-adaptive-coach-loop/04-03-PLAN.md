---
phase: 04-adaptive-coach-loop
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/app/analyze/results-dashboard-view-model.ts
  - src/app/analyze/results-dashboard-view-model.test.ts
  - src/app/analyze/results-dashboard.tsx
  - src/app/analyze/results-dashboard.contract.test.ts
  - src/app/analyze/analysis.module.css
  - src/app/history/[id]/coach-protocol-outcome-panel.tsx
  - src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts
  - src/app/history/[id]/page.tsx
  - src/app/history/page.tsx
  - src/app/history/page.contract.test.ts
  - src/actions/history.ts
  - src/actions/history.test.ts
  - src/actions/dashboard.ts
  - src/actions/dashboard.test.ts
  - src/app/dashboard/dashboard-truth-view-model.ts
  - src/app/dashboard/dashboard-truth-view-model.test.ts
  - src/app/dashboard/page.tsx
  - src/app/dashboard/page.contract.test.ts
  - src/app/copy-claims.contract.test.ts
autonomous: true
requirements: [COACH-01, COACH-03, COACH-04]
user_setup: []
must_haves:
  truths:
    - "D-04: Pending feedback prompts closure without blocking normal use."
    - "D-05: Outcome save does not require immediate clip, but shows compatible validation CTA."
    - "D-06: Improved report plus worse compatible validation displays explicit conflict and asks for short validation."
    - "D-07: UI writes structured reason codes plus optional note."
    - "D-10: completed without result is neutral and asks user to close the result."
    - "D-11: invalid_capture requires a reason and becomes learning."
    - "D-12: Outcome corrections remain auditable."
    - "D-21: worse with invalid capture is not counted against protocol and asks for new validation."
    - "D-22: Repeated completed-without-result prompts closure before coach advances too far."
    - "D-23: Outcome/trend conflict blocks aggressive CTA and routes to validation."
    - "D-25: Partial progress is visible without over-consolidating."
    - "D-26: Analysis shows essential reason, dashboard short active memory, history complete audit."
    - "D-37: Post-analysis shows only essential coach loop."
    - "D-38: Dashboard shows active loop, pending feedback, next validation, and short memory summary."
    - "D-39: History is the audit surface for outcomes, revisions, conflicts, snapshots, compatible clips, checkpoints, and memory explanations."
    - "D-41: Coach copy is direct, demanding, premium, and honest without perfection/rank/guarantee claims."
    - "D-42: UI/action tests cover outcome handling, historical conflict, and memory behavior."
  artifacts:
    - path: "src/app/analyze/results-dashboard-view-model.ts"
      provides: "Compact post-analysis coach loop model with primary focus, one protocol, status, evidence, and CTA."
      contains: "buildAdaptiveCoachLoopModel"
    - path: "src/app/history/[id]/coach-protocol-outcome-panel.tsx"
      provides: "Segmented outcome capture and correction UI with reason codes, note, receipt, and validation CTA."
      contains: "CoachProtocolOutcomePanel"
    - path: "src/actions/dashboard.ts"
      provides: "Dashboard active-loop read model from pending/latest protocol outcomes and memory summary."
      contains: "activeCoachLoop"
  key_links:
    - from: "src/app/history/[id]/coach-protocol-outcome-panel.tsx"
      to: "src/actions/history.ts"
      via: "client panel calls recordCoachProtocolOutcome and refreshes history/dashboard state"
      pattern: "recordCoachProtocolOutcome"
    - from: "src/actions/dashboard.ts"
      to: "src/db/schema.ts"
      via: "dashboard queries coachProtocolOutcomes for pending started/completed and conflict states"
      pattern: "coachProtocolOutcomes"
---

<objective>
Add the adaptive coach loop surfaces in analysis, dashboard, and history.

Purpose: make the loop visible where it belongs: essential next action after analysis, active pending/validation state on dashboard, and full audit with revisions/conflicts in history.
Output: view models, outcome panel, dashboard/history read models, responsive styling, and copy/contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adaptive-coach-loop/04-CONTEXT.md
@.planning/phases/04-adaptive-coach-loop/04-RESEARCH.md
@.planning/phases/04-adaptive-coach-loop/04-UI-SPEC.md
@.planning/phases/04-adaptive-coach-loop/04-VALIDATION.md
@.planning/phases/04-adaptive-coach-loop/04-01-SUMMARY.md
@.planning/phases/04-adaptive-coach-loop/04-02-SUMMARY.md
@src/app/analyze/results-dashboard.tsx
@src/app/analyze/results-dashboard-view-model.ts
@src/app/history/[id]/sensitivity-acceptance-panel.tsx
@src/app/history/[id]/page.tsx
@src/app/history/page.tsx
@src/actions/dashboard.ts
@src/app/dashboard/dashboard-truth-view-model.ts
</context>

<threat_model>
Scope: client controls and server-backed read models for outcome capture and active coach loop state.

Threats:
- HIGH: client sends forged protocol/focus/status payload. Mitigation: server action revalidates ownership and all status/reason/focus/protocol fields; UI never becomes the authority.
- MEDIUM: UI hides a conflict or pending closure state and makes the coach seem more certain than it is. Mitigation: view-model tests assert conflict/pending/invalid states render visible warning or closure copy.
- MEDIUM: note/error text shifts layout or creates accessibility traps. Mitigation: fixed control targets, adjacent error text, aria-live receipts, and focus return after successful write.
- LOW: dashboard leaks another user's active loop. Mitigation: dashboard action filters by authenticated user only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build and render the compact post-analysis coach loop</name>
  <files>src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard-view-model.test.ts, src/app/analyze/results-dashboard.tsx, src/app/analyze/results-dashboard.contract.test.ts, src/app/analyze/analysis.module.css</files>
  <read_first>src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard-view-model.test.ts, src/app/analyze/results-dashboard.tsx, src/app/analyze/results-dashboard.contract.test.ts, src/app/analyze/analysis.module.css, .planning/phases/04-adaptive-coach-loop/04-UI-SPEC.md, src/types/engine.ts</read_first>
  <action>
Add an essential adaptive coach loop block to the analysis result surface.

Model requirements:
- one coach verdict/status;
- primary focus title and why-now;
- up to two secondary focus chips;
- exactly one next-block protocol and duration;
- tier, confidence, coverage, and memory/conflict reason badges;
- CTA state:
  - no saved session id: `Gravar analise para registrar resultado`;
  - pending outcome: `Registrar resultado do bloco`;
  - outcome recorded but unvalidated: `Gravar validacao compativel`;
  - conflict: validation CTA and warning copy.

Render requirements:
- keep existing verdict/mastery first;
- show this coach loop before deep technical audit;
- do not render revision timeline here;
- use existing `glass-card`, `.btn`, badges, and `analysis.module.css`;
- no nested cards;
- pt-BR copy from UI-SPEC, ASCII-safe if surrounding files use ASCII fallback;
- no perfect sensitivity, guaranteed improvement, rank, or final-certainty claims.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard-view-model.test.ts src/app/analyze/results-dashboard.contract.test.ts src/app/copy-claims.contract.test.ts</verify>
  <acceptance_criteria>
    - View-model exposes `primaryFocus`, `secondaryFocuses`, `nextBlock`, and coach-loop CTA.
    - Contract tests assert verdict/mastery appears before adaptive coach loop and technical audit appears after it.
    - No saved session state shows `Gravar analise para registrar resultado`.
    - Conflict state includes visible warning and routes to compatible validation.
    - Copy-claims test scans new coach-loop strings.
  </acceptance_criteria>
  <done>Post-analysis gives the user the next coach-loop action without becoming the audit page.</done>
</task>

<task type="auto">
  <name>Task 2: Add protocol outcome capture and correction UI</name>
  <files>src/app/history/[id]/coach-protocol-outcome-panel.tsx, src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts, src/app/history/[id]/page.tsx, src/actions/history.ts, src/actions/history.test.ts</files>
  <read_first>src/app/history/[id]/sensitivity-acceptance-panel.tsx, src/app/history/[id]/sensitivity-acceptance-panel.contract.test.ts, src/app/history/[id]/page.tsx, src/actions/history.ts, src/actions/history.test.ts, src/core/coach-outcomes.ts, .planning/phases/04-adaptive-coach-loop/04-UI-SPEC.md</read_first>
  <action>
Create `CoachProtocolOutcomePanel` for saved analysis detail.

Interaction requirements:
- segmented/button-group control with visible labels:
  - Comecei o bloco
  - Completei sem medir
  - Melhorou no treino
  - Ficou igual
  - Piorou no treino
  - Captura invalida
- selecting `Captura invalida` requires at least one reason code before submit.
- reason checklist labels match UI-SPEC: capture quality, incompatible context, poor execution, variable changed, confusing protocol, fatigue/pain, other.
- optional note is plain text and length-limited.
- submit button uses `Registrar resultado do bloco`; loading label is `Registrando resultado...`.
- successful write replaces form with receipt, aria-live polite status, and `Gravar validacao compativel`.
- corrections use `Corrigir resultado e manter revisao auditavel` and create a revision row through the server action.
- action errors render adjacent to the failed control.

Integrate the panel on `src/app/history/[id]/page.tsx` near the coach plan/next block. Keep the old sensitivity panel only if it remains useful as a legacy fallback; do not show duplicate competing CTAs for the same protocol.
  </action>
  <verify>npx vitest run src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts src/actions/history.test.ts</verify>
  <acceptance_criteria>
    - Panel renders all six outcome options with visible text.
    - `invalid_capture` cannot submit without a reason.
    - Successful result shows `Gravar validacao compativel`.
    - Correction path references auditable revision instead of deletion.
    - Page contract proves the panel appears on saved analysis detail when coachPlan exists.
  </acceptance_criteria>
  <done>Users can close the coach loop with quick structured feedback and auditable corrections.</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard active loop and history audit surfaces</name>
  <files>src/actions/history.ts, src/actions/history.test.ts, src/actions/dashboard.ts, src/actions/dashboard.test.ts, src/app/dashboard/dashboard-truth-view-model.ts, src/app/dashboard/dashboard-truth-view-model.test.ts, src/app/dashboard/page.tsx, src/app/dashboard/page.contract.test.ts, src/app/history/page.tsx, src/app/history/page.contract.test.ts, src/app/history/[id]/page.tsx</files>
  <read_first>src/actions/dashboard.ts, src/actions/history.test.ts, src/app/dashboard/dashboard-truth-view-model.ts, src/app/dashboard/dashboard-truth-view-model.test.ts, src/app/dashboard/page.tsx, src/app/dashboard/page.contract.test.ts, src/actions/history.ts, src/app/history/page.tsx, src/app/history/page.contract.test.ts, src/app/history/[id]/page.tsx, src/db/schema.ts</read_first>
  <action>
Expose the active coach loop in dashboard and full audit in history.

Dashboard behavior:
- Add `activeCoachLoop` or equivalent to `DashboardStats`.
- If a protocol is pending or started, dashboard next action becomes `Fechar protocolo pendente`.
- If outcome and precision trend conflict, render a warning band and validation CTA, not a stronger coach action.
- If no protocol exists, preserve existing dashboard next-action behavior.
- Keep principal precision trend and latest coach block visible but concise.

History behavior:
- History list shows compact status chips for latest protocol outcome per session: pending, started, completed, improved self-report, unchanged, worse, invalid capture, conflict.
- History detail shows coach snapshot, outcome records, revisions, conflicts, compatible clip/checkpoint context, and memory explanation.
- Full revision timeline belongs in history detail, not analysis or dashboard.
- Add contract tests for pending closure copy, conflict copy, revision timeline, and `Ver auditoria do coach`.
  </action>
  <verify>npx vitest run src/actions/history.test.ts src/actions/dashboard.test.ts src/app/dashboard/dashboard-truth-view-model.test.ts src/app/dashboard/page.contract.test.ts src/app/history/page.contract.test.ts</verify>
  <acceptance_criteria>
    - Dashboard stats include active coach loop state filtered by authenticated user.
    - Dashboard conflict state uses validation copy and never promotes apply_protocol.
    - History list shows outcome status chips.
    - History detail shows revision/audit timeline and memory explanation.
    - Existing precision trend dashboard/history tests remain green.
  </acceptance_criteria>
  <done>The adaptive coach loop has the right visibility: small after analysis, active on dashboard, complete in history.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/app/analyze/results-dashboard-view-model.test.ts src/app/analyze/results-dashboard.contract.test.ts src/app/history/[id]/coach-protocol-outcome-panel.contract.test.ts src/actions/history.test.ts src/actions/dashboard.test.ts src/app/dashboard/dashboard-truth-view-model.test.ts src/app/dashboard/page.contract.test.ts src/app/history/page.contract.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- Post-analysis shows only the essential coach loop and correct CTA.
- Users can record and correct outcomes with accessible segmented controls.
- Dashboard surfaces pending/conflict/validation state without full audit noise.
- History contains outcome revisions, conflicts, coach snapshots, precision checkpoints, and memory explanations.
- UI copy remains premium and honest without impossible claims.

</success_criteria>

<output>
After completion, create `.planning/phases/04-adaptive-coach-loop/04-03-SUMMARY.md`
</output>
