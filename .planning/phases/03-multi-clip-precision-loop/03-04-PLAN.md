---
phase: 03-multi-clip-precision-loop
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/actions/history.ts
  - src/actions/history.test.ts
  - src/actions/dashboard.ts
  - src/app/history/page.tsx
  - src/app/history/page.contract.test.ts
  - src/app/history/[id]/page.tsx
  - src/app/dashboard/dashboard-truth-view-model.ts
  - src/app/dashboard/dashboard-truth-view-model.test.ts
  - src/app/dashboard/page.tsx
  - src/app/dashboard/page.contract.test.ts
  - src/app/copy-claims.contract.test.ts
autonomous: true
requirements: [PREC-03]
user_setup: []
must_haves:
  truths:
    - "D-20: The product should maintain an active evolution line per compatible context, not just compare loose sessions."
    - "D-21: An active line contains baseline, current status, priority, last block, next validation, valid clips, and blocked clips."
    - "D-22: Important variable changes restart the line with a new baseline; the old line remains auditable but cannot mix into the new trend."
    - "D-24: Each compatible validation creates a checkpoint."
    - "D-25: Checkpoint states include baseline created, initial signal, in validation, validated progress, validated regression, oscillation, and consolidated."
    - "D-30: The UI should expose the experimental discipline simply as variavel em teste and proxima validacao."
    - "D-31: The line should feel like an active mini-program per compatible context: objective, variable in test, current checkpoint, next block, and next validation."
    - "D-34: The line should have a checkpoint timeline. Analysis/dashboard show a short summary; history shows the complete timeline."
    - "D-38: History is the audit surface. It should show compatible groups, baselines, windows, blocked clips, reasons, and checkpoint timelines."
    - "D-39: Compatible groups in history should be clickable, one group per strict context such as Beryl/red-dot/patch/distance/loadout."
    - "D-40: Dashboard is the executive evolution surface. It should show the current principal trend, next action, and general context without becoming the full technical audit."
    - "D-41: When a clip is not comparable, the reason must appear in both analysis and history."
    - "D-42: The CTA for continuing the loop should be explicit: Gravar validacao compativel."
    - "D-43: Multi-clip evidence must be visible to the user and used by the coach."
    - "D-44: Phase 3 should produce a structured trend summary for Phase 4."
    - "D-46: If trend validates progress, the coach should consolidate before changing a variable."
    - "D-47: If trend validates regression, the coach should return to the last reliable baseline and request compatible validation."
    - "D-49: The coach must cite compatible history explicitly with a clear summary."
    - "D-50: Recurrence in compatible clips drives priority unless a critical blocker overrides it."
    - "D-58: If sensitivity and training point in different directions, multi-clip evidence decides the priority."
  artifacts:
    - path: "src/app/history/page.tsx"
      provides: "History audit surface for compatible groups, blocked clips, and checkpoint timeline summaries."
      contains: "precision"
    - path: "src/actions/dashboard.ts"
      provides: "Dashboard stats include principal precision trend and next validation context."
      contains: "principalPrecisionTrend"
    - path: "src/app/dashboard/dashboard-truth-view-model.ts"
      provides: "Executive copy for current principal trend without full audit detail."
      contains: "precisionTrend"
  key_links:
    - from: "src/actions/history.ts"
      to: "src/db/schema.ts"
      via: "reads precision evolution line/checkpoint payloads for history cards"
      pattern: "precisionEvolutionLines"
    - from: "src/actions/dashboard.ts"
      to: "src/core/precision-loop.ts"
      via: "dashboard chooses current principal line and preserves strict trend labels"
      pattern: "PrecisionTrendSummary"
---

<objective>
Add the history audit and dashboard executive precision surfaces.

Purpose: make compatible groups, baselines, checkpoint timelines, blocked reasons, and principal trend visible in the right places. History becomes the audit page; dashboard becomes the concise next-action page.
Output: action data, history cards/details, dashboard view model/page updates, and copy/contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-clip-precision-loop/03-CONTEXT.md
@.planning/phases/03-multi-clip-precision-loop/03-RESEARCH.md
@.planning/phases/03-multi-clip-precision-loop/03-01-SUMMARY.md
@.planning/phases/03-multi-clip-precision-loop/03-02-SUMMARY.md
@src/actions/history.ts
@src/actions/dashboard.ts
@src/app/history/page.tsx
@src/app/history/[id]/page.tsx
@src/app/dashboard/dashboard-truth-view-model.ts
@src/app/dashboard/page.tsx
@src/types/engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose precision lines and checkpoint summaries to history</name>
  <files>src/actions/history.ts, src/actions/history.test.ts, src/app/history/page.tsx, src/app/history/page.contract.test.ts</files>
  <read_first>src/actions/history.ts, src/db/schema.ts, src/app/history/page.tsx, src/app/history/page.contract.test.ts</read_first>
  <action>
Extend history data to include precision line groups.

Implementation guidance:
- Add a read path such as `getPrecisionHistoryLines` or extend `getHistorySessions` if that keeps the API small.
- Return active/archived line summaries with compatibility key, readable context, status label, variable in test, next validation, valid clip count, blocked clip count, baseline/current dates, latest trend label, and checkpoint previews.
- Include blocked clips and blocker reasons in the line payload or card summary.
- Keep existing session list and field feedback summaries working.
- Make compatible groups clickable. A query-param-driven selected line on `/history?line=...` is acceptable if a new route is too much; the key is that users can inspect one strict group and its checkpoints.
- Add page contract tests for compatible groups, blocked reasons, checkpoint timeline language, and CTA `Gravar validacao compativel`.
  </action>
  <verify>npx vitest run src/actions/history.test.ts src/app/history/page.contract.test.ts</verify>
  <acceptance_criteria>
    - History page exposes compatible precision groups.
    - Each group shows strict context, valid clips, blocked clips, and latest status.
    - Blocked clip reasons are visible in history.
    - Groups are clickable or selectable for audit detail.
    - Existing history session list remains available.
  </acceptance_criteria>
  <done>History becomes the audit surface for strict compatible evolution lines.</done>
</task>

<task type="auto">
  <name>Task 2: Add checkpoint context to history detail</name>
  <files>src/app/history/[id]/page.tsx, src/app/history/page.contract.test.ts, src/actions/history.ts, src/actions/history.test.ts</files>
  <read_first>src/app/history/[id]/page.tsx, src/app/history/page.tsx, src/actions/history.ts</read_first>
  <action>
Show the saved session's precision checkpoint context on the history detail page.

Requirements:
- show the line label/context if the session belongs to a precision line;
- show checkpoint state, variable in test, next validation, and whether this clip advanced or blocked the line;
- if blocked, show blocker reasons;
- keep existing `SensitivityAcceptancePanel` and `ResultsDashboard` behavior intact.

Avoid duplicating the entire history timeline on detail if the page would become too heavy; a compact checkpoint card with a link back to the line audit is enough.
  </action>
  <verify>npx vitest run src/app/history/page.contract.test.ts src/app/history/analysis-result-hydration.test.ts</verify>
  <acceptance_criteria>
    - History detail can show checkpoint state for the current session.
    - Blocked checkpoint reasons appear when the session is not comparable.
    - Existing sensitivity acceptance and analysis dashboard remain ordered correctly.
  </acceptance_criteria>
  <done>Saved sessions explain how they affected the strict precision loop.</done>
</task>

<task type="auto">
  <name>Task 3: Promote principal trend to dashboard next action</name>
  <files>src/actions/dashboard.ts, src/app/dashboard/dashboard-truth-view-model.ts, src/app/dashboard/dashboard-truth-view-model.test.ts, src/app/dashboard/page.tsx, src/app/dashboard/page.contract.test.ts</files>
  <read_first>src/actions/dashboard.ts, src/app/dashboard/dashboard-truth-view-model.ts, src/app/dashboard/page.tsx, src/core/precision-loop.ts</read_first>
  <action>
Update dashboard to consume principal precision trend.

Behavior:
- `DashboardStats` should expose `principalPrecisionTrend` or equivalent from active precision lines and/or latest saved precision trend.
- Dashboard next action should prefer precision trend context when available:
  - baseline/initial signal -> record compatible validation;
  - in validation/oscillation -> keep variable fixed and validate;
  - validated progress -> consolidate before changing variable;
  - validated regression -> return to reliable baseline and validate;
  - not comparable -> recapture or align context.
- Keep the dashboard concise. Do not render the full checkpoint timeline here.
- Existing latest mastery and coach next block remain visible.
- Add tests that dashboard positive raw deltas cannot override precision `oscillation` or `not_comparable`.
  </action>
  <verify>npx vitest run src/app/dashboard/dashboard-truth-view-model.test.ts src/app/dashboard/page.contract.test.ts</verify>
  <acceptance_criteria>
    - Dashboard stats include principal precision trend context.
    - Dashboard view model routes each trend label to conservative next action copy.
    - Raw score delta cannot claim progress when precision trend says oscillation or not comparable.
    - Dashboard still shows confidence/coverage and no overclaiming language.
  </acceptance_criteria>
  <done>Dashboard becomes the executive precision loop surface without becoming the audit page.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/actions/history.test.ts src/app/history/page.contract.test.ts src/app/dashboard/dashboard-truth-view-model.test.ts src/app/dashboard/page.contract.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- History shows strict compatible groups, baseline/current context, blocked clips, blocker reasons, and checkpoint timeline summaries.
- History detail explains how the current saved clip affected its precision line.
- Dashboard shows the principal precision trend, next validation action, and conservative trend language.
- Raw score deltas cannot override strict precision trend labels.
- Phase 4 coach has visible and structured recent compatible evidence to cite.

</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-clip-precision-loop/03-04-SUMMARY.md`
</output>

