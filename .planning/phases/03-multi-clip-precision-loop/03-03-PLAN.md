---
phase: 03-multi-clip-precision-loop
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/app/analyze/results-dashboard-view-model.ts
  - src/app/analyze/results-dashboard-view-model.test.ts
  - src/app/analyze/results-dashboard.tsx
  - src/app/analyze/results-dashboard.contract.test.ts
  - src/app/analyze/analysis.module.css
  - src/app/copy-claims.contract.test.ts
autonomous: true
requirements: [PREC-03]
user_setup: []
must_haves:
  truths:
    - "D-09: UI should frame strict blocking as controle de precisao, not product failure."
    - "D-15: Trend labels are conservative and evidence-based."
    - "D-35: The main moment where the user feels the loop is immediately after analyzing/saving a new clip."
    - "D-36: Post-analysis should show the clip verdict first, then the trend block, then technical details."
    - "D-37: The trend block after analysis should show verdict plus essential deltas, not a giant audit report."
    - "D-41: When a clip is not comparable, the reason must appear in both analysis and history."
    - "D-42: The CTA for continuing the loop should be explicit: Gravar validacao compativel."
    - "D-43: Multi-clip evidence must be visible to the user and used by the coach."
    - "D-44: Phase 3 should produce a structured trend summary for Phase 4."
    - "D-45: If the current clip conflicts with strict trend evidence, the coach must lower aggressiveness and ask for validation."
    - "D-48: The coach must explain when it is not aggressive: too few clips, conflict, weak quality, missing baseline, regression, or oscillation."
    - "D-51: When trend changes coach priority, the UI must show a visible reason."
    - "D-52: Sinal inicial should drive controlled validation, not consolidation or aggressive variable changes."
    - "D-53: The coach output should separate measured evidence, coach reading, and next block."
    - "D-54: Every next block must be validatable by a new compatible clip."
    - "D-55: Coach voice should be direct, premium, and demanding. Avoid hype and vague motivation."
    - "D-56: With insufficient evidence, the coach prioritizes recapture/validation before a new training block."
    - "D-57: Coach, training, and sensitivity are the premium core of the product."
    - "D-59: Sensitivity recommendations must become stricter."
    - "D-60: Training must always connect to a measured blocker."
    - "D-61: Each training block should be complete and validatable by a new compatible clip."
  artifacts:
    - path: "src/app/analyze/results-dashboard-view-model.ts"
      provides: "Compact post-analysis precision trend UI model with label, deltas, blockers, next validation, and CTA."
      contains: "buildPrecisionTrendBlockModel"
    - path: "src/app/analyze/results-dashboard.tsx"
      provides: "Trend block immediately after verdict and before technical details."
      contains: "precisionTrend"
    - path: "src/app/analyze/results-dashboard.contract.test.ts"
      provides: "Contract tests for verdict-first then trend block order and no overclaiming."
  key_links:
    - from: "src/app/analyze/results-dashboard.tsx"
      to: "src/types/engine.ts"
      via: "activeSession.precisionTrend powers the compact trend block"
      pattern: "precisionTrend"
    - from: "src/app/analyze/results-dashboard-view-model.ts"
      to: "src/core/precision-loop.ts"
      via: "label formatting and blocker summaries use shared core helpers"
      pattern: "formatPrecisionTrendLabel"
---

<objective>
Add the post-analysis precision loop experience.

Purpose: make the user feel the multi-clip loop immediately after saving/analyzing a clip without turning the analysis page into the full audit surface.
Output: compact trend block after verdict, essential deltas, blocked reasons, next validation CTA, styling, and copy/contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-clip-precision-loop/03-CONTEXT.md
@.planning/phases/03-multi-clip-precision-loop/03-RESEARCH.md
@.planning/phases/03-multi-clip-precision-loop/03-01-SUMMARY.md
@.planning/phases/03-multi-clip-precision-loop/03-02-SUMMARY.md
@src/types/engine.ts
@src/core/precision-loop.ts
@src/app/analyze/results-dashboard-view-model.ts
@src/app/analyze/results-dashboard.tsx
@src/app/analyze/analysis.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build compact precision trend view model</name>
  <files>src/app/analyze/results-dashboard-view-model.ts, src/app/analyze/results-dashboard-view-model.test.ts</files>
  <read_first>src/app/analyze/results-dashboard-view-model.ts, src/types/engine.ts, src/core/precision-loop.ts</read_first>
  <action>
Add `buildPrecisionTrendBlockModel` or equivalent.

The model should expose:
- label and tone for baseline, initial signal, in validation, validated progress, validated regression, oscillation, not comparable, and consolidated;
- compact body copy;
- compatible clip count;
- actionable delta and mechanical delta;
- pillar delta chips for control, consistency, confidence, and clip quality;
- evidence summary using coverage/confidence/evidence level;
- blocker reasons when not comparable;
- a visible reason when coach/sensitivity should stay conservative;
- CTA label exactly `Gravar validacao compativel`.

Copy rules:
- one clip = baseline only;
- two clips = signal to validate;
- progress/regression only when trend label says so;
- not comparable is framed as precision control;
- no perfect, guaranteed, final, or rank-improvement claims.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - View-model tests cover baseline, initial signal, validated progress, validated regression, oscillation, and not comparable.
    - Not comparable includes blocker reasons.
    - CTA label is `Gravar validacao compativel`.
    - Copy tests do not contain perfect/guaranteed language.
  </acceptance_criteria>
  <done>The analysis UI has a small, deterministic model for the trend block.</done>
</task>

<task type="auto">
  <name>Task 2: Render trend block after the clip verdict</name>
  <files>src/app/analyze/results-dashboard.tsx, src/app/analyze/results-dashboard.contract.test.ts, src/app/analyze/analysis.module.css</files>
  <read_first>src/app/analyze/results-dashboard.tsx, src/app/analyze/results-dashboard.contract.test.ts, src/app/analyze/analysis.module.css</read_first>
  <action>
Render the precision trend block after the existing mastery verdict section and before metric/technical audit sections.

UI requirements:
- Keep verdict first.
- Trend block shows label, compatible count, essential deltas, pillar delta chips, evidence summary, and CTA.
- If current clip is blocked from precise trend, show blocker reasons plainly.
- Keep the full detailed audit below. Do not duplicate the history timeline here.
- Avoid nested cards if the existing layout can use one section wrapper and internal rows.
- Keep responsive text within containers on mobile.
- Add contract tests that assert the source order: mastery verdict before precision trend before technical details.
  </action>
  <verify>npx vitest run src/app/analyze/results-dashboard.contract.test.ts src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - Results dashboard references `activeSession.precisionTrend`.
    - Trend block renders after `Leitura de mastery` or existing verdict marker.
    - Trend block contains `Gravar validacao compativel`.
    - Blocked trend reasons are visible in the component.
    - Existing verdict, evidence badge, pillar, coach, and tracking contract tests still pass.
  </acceptance_criteria>
  <done>The post-analysis page exposes the precision loop without hiding the technical audit.</done>
</task>

<task type="auto">
  <name>Task 3: Protect trend copy and validation language</name>
  <files>src/app/copy-claims.contract.test.ts, src/app/analyze/results-dashboard.contract.test.ts, src/app/analyze/results-dashboard-view-model.test.ts</files>
  <read_first>src/app/copy-claims.contract.test.ts, src/app/analyze/results-dashboard.contract.test.ts</read_first>
  <action>
Extend copy and contract tests for the new trend block.

Test requirements:
- trend copy can say progress/regression only for validated labels;
- baseline and initial signal copy ask for compatible validation;
- not comparable copy explains blocker reasons and avoids product-failure language;
- `Gravar validacao compativel` appears in the analysis surface;
- product copy scan includes any new files or strings if needed.
  </action>
  <verify>npx vitest run src/app/copy-claims.contract.test.ts src/app/analyze/results-dashboard.contract.test.ts src/app/analyze/results-dashboard-view-model.test.ts</verify>
  <acceptance_criteria>
    - New trend copy is covered by copy claim guardrails.
    - Baseline/initial signal cannot be phrased as validated progress.
    - Not comparable is treated as strict precision control.
  </acceptance_criteria>
  <done>The post-analysis precision loop stays premium and honest under contract tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/app/analyze/results-dashboard-view-model.test.ts src/app/analyze/results-dashboard.contract.test.ts src/app/copy-claims.contract.test.ts
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- Post-analysis shows clip verdict first and precision trend second.
- Trend block shows essential deltas, evidence, blockers, and validation CTA.
- Incompatible clips explain why precise trend is blocked.
- UI copy avoids perfect sensitivity, guaranteed improvement, and false progress.
- Existing analysis details remain available below the compact trend block.

</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-clip-precision-loop/03-03-SUMMARY.md`
</output>

