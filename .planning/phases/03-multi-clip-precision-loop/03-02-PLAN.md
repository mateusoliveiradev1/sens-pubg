---
phase: 03-multi-clip-precision-loop
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/db/schema.ts
  - drizzle/0008_precision_evolution_lines.sql
  - src/db/schema.test.ts
  - src/actions/history.ts
  - src/actions/history.test.ts
  - src/app/history/analysis-result-hydration.ts
  - src/app/history/analysis-result-hydration.test.ts
  - src/core/coach-memory.ts
  - src/core/coach-memory.test.ts
autonomous: true
requirements: [PREC-03]
user_setup: []
must_haves:
  truths:
    - "D-20: The product should maintain an active evolution line per compatible context, not just compare loose sessions."
    - "D-21: An active line contains baseline, current status, priority, last block, next validation, valid clips, and blocked clips."
    - "D-22: Important variable changes restart the line with a new baseline; the old line remains auditable but cannot mix into the new trend."
    - "D-23: Variables that break or restart the line include patch, weapon, scope, stance, attachments, distance, and strong sensitivity changes."
    - "D-24: Each compatible validation creates a checkpoint."
    - "D-25: Checkpoint states include baseline created, initial signal, in validation, validated progress, validated regression, oscillation, and consolidated."
    - "D-26: The active line must have a short measurable goal for the next checkpoint."
    - "D-27: The loop must use one primary variable in test per checkpoint."
    - "D-28: The variable in test is explicit and mandatory: sensitivity, vertical control, horizontal noise, consistency, capture quality, loadout, or validation."
    - "D-29: If the user changes a different variable before validating the current one, the line breaks and starts a new baseline."
    - "D-30: The UI should expose the experimental discipline simply as variavel em teste and proxima validacao."
    - "D-31: The line should feel like an active mini-program per compatible context: objective, variable in test, checkpoint, next block, and next validation."
    - "D-32: Maturity stages should show evolution without lying: Baseline criado -> Sinal inicial -> Em validacao -> Progresso validado -> Consolidado."
    - "D-33: When a line reaches Consolidado, the coach should choose the next variable based on evidence."
    - "D-34: The line should have a checkpoint timeline. Analysis/dashboard show a short summary; history shows the complete timeline."
    - "D-43: Multi-clip evidence must be visible to the user and used by the coach."
    - "D-44: Phase 3 should produce a structured trend summary for Phase 4: compatible clip count, baseline, window, label, deltas, pillars, recurring diagnoses, blockers, and evidence level."
    - "D-45: If the current clip conflicts with strict trend evidence, the coach must lower aggressiveness and ask for validation."
    - "D-46: If trend validates progress, the coach should consolidate before changing a variable."
    - "D-47: If trend validates regression, the coach should return to the last reliable baseline and request compatible validation."
    - "D-48: The coach must explain when it is not aggressive: too few clips, conflict, weak quality, missing baseline, regression, or oscillation."
    - "D-49: The coach must cite compatible history explicitly with a clear summary."
    - "D-50: Recurrence in compatible clips drives priority unless a critical blocker overrides it."
    - "D-51: When trend changes coach priority, the UI must show a visible reason."
    - "D-52: Sinal inicial should drive controlled validation, not consolidation or aggressive variable changes."
    - "D-53: The coach output should separate measured evidence, coach reading, and next block."
    - "D-54: Every next block must be validatable by a new compatible clip."
    - "D-55: Coach voice should be direct, premium, and demanding. Avoid hype and vague motivation."
    - "D-56: With insufficient evidence, the coach prioritizes recapture/validation before a new training block."
    - "D-57: Coach, training, and sensitivity are the premium core: measured evidence -> sensitivity decision -> training block -> next compatible validation."
    - "D-58: If sensitivity and training point in different directions, multi-clip evidence decides the priority."
    - "D-59: Sensitivity recommendations must become stricter and can lead only with strong evidence or compatible trend support."
    - "D-60: Training must always connect to a measured blocker."
    - "D-61: Each training block should be complete: objective, where/mode, duration, exercise, execution, target, stop criteria, and compatible validation clip."
  artifacts:
    - path: "src/actions/history.ts"
      provides: "Save-time precision trend resolution, active line update, checkpoint creation, and stored fullResult precision summary."
      contains: "precisionTrend"
    - path: "src/db/schema.ts"
      provides: "Precision evolution line and checkpoint tables for active compatible context state."
      contains: "precisionEvolutionLines"
    - path: "src/core/coach-memory.ts"
      provides: "Strict compatible trend evidence in coach memory without weakening existing sensitivity convergence."
      contains: "precisionTrend"
  key_links:
    - from: "src/actions/history.ts"
      to: "src/core/precision-loop.ts"
      via: "saveAnalysisResult resolves trend after coachPlan/mastery and before inserting/updating line state"
      pattern: "resolvePrecisionTrend"
    - from: "src/app/history/analysis-result-hydration.ts"
      to: "src/types/engine.ts"
      via: "legacy fullResult records validate/backfill precision trend safely"
      pattern: "PrecisionTrendSummary"
---

<objective>
Persist active evolution lines, checkpoints, and a coach-ready precision trend handoff.

Purpose: make Phase 3 durable across saved analyses. Each new saved clip should either update the strict active line, create/restart a baseline, or record why it was blocked from precise trend comparison.
Output: schema/migration, save integration, hydration validation, coach memory handoff, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-clip-precision-loop/03-CONTEXT.md
@.planning/phases/03-multi-clip-precision-loop/03-RESEARCH.md
@.planning/phases/03-multi-clip-precision-loop/03-01-SUMMARY.md
@src/core/precision-loop.ts
@src/types/engine.ts
@src/db/schema.ts
@src/actions/history.ts
@src/core/coach-memory.ts
@src/app/history/analysis-result-hydration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add precision evolution line schema</name>
  <files>src/db/schema.ts, drizzle/0008_precision_evolution_lines.sql, src/db/schema.test.ts</files>
  <read_first>src/db/schema.ts, drizzle/0007_community_gamification_core.sql, src/db/schema.test.ts, .planning/codebase/CONVENTIONS.md</read_first>
  <action>
Add a minimal normalized persistence layer for active precision lines.

Recommended tables:
- `precision_evolution_lines`
  - id uuid primary key
  - user_id uuid not null references users on delete cascade
  - compatibility_key text not null
  - status text not null
  - variable_in_test text not null
  - baseline_session_id uuid references analysis_sessions on delete set null
  - current_session_id uuid references analysis_sessions on delete set null
  - valid_clip_count integer not null default 0
  - blocked_clip_count integer not null default 0
  - payload jsonb not null default '{}'
  - created_at timestamp default now not null
  - updated_at timestamp default now not null
  - unique index on user_id + compatibility_key for the active line
- `precision_checkpoints`
  - id uuid primary key
  - line_id uuid not null references precision_evolution_lines on delete cascade
  - analysis_session_id uuid references analysis_sessions on delete set null
  - state text not null
  - variable_in_test text not null
  - payload jsonb not null default '{}'
  - created_at timestamp default now not null
  - indexes for line_id/created_at and analysis_session_id

Use JSON payloads for trend summary, blockers, next validation, and timeline details so Phase 3 remains lean. Add schema contract tests for required columns, foreign keys, and unique/index expectations.
  </action>
  <verify>npx vitest run src/db/schema.test.ts</verify>
  <acceptance_criteria>
    - Schema exports `precisionEvolutionLines` and `precisionCheckpoints`.
    - Migration creates both tables and useful indexes.
    - Schema tests lock user/session foreign keys and active-line unique key.
    - Existing schema tests remain green.
  </acceptance_criteria>
  <done>Active precision lines and checkpoints have durable storage without over-modeling every field.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve and persist precision trend on analysis save</name>
  <files>src/actions/history.ts, src/actions/history.test.ts, src/app/history/analysis-result-hydration.ts, src/app/history/analysis-result-hydration.test.ts</files>
  <read_first>src/actions/history.ts, src/core/precision-loop.ts, src/app/history/analysis-result-hydration.ts, src/actions/history.test.ts</read_first>
  <action>
Integrate precision trend resolution into `saveAnalysisResult`.

Implementation guidance:
- Fetch enough recent prior sessions to resolve strict trend, including fullResult, patch, weapon, scope, stance, attachments, distance, and createdAt.
- Use `resolvePrecisionTrend` after `coachPlan` and `mastery` are built so current evidence is complete.
- Attach `precisionTrend` to `enrichedResult.fullResult`.
- Insert the analysis session first if a session ID is needed by checkpoint payloads, then upsert/update the active line and checkpoint.
- If current clip is compatible, update valid clip count, baseline/current session references, status, variable in test, next validation, and payload.
- If current clip is not comparable, store blocked reason in the line/checkpoint payload without mixing it into trend math.
- If key-breaking variables changed, create or use a new line key and baseline rather than mutating the old line.
- Preserve existing sensitivity history and coach plan behavior.
- Add hydration validation for stored `precisionTrend`; malformed summaries should be dropped or recomputed from available recent context only when safe. Do not invent validated progress from a single legacy result.

Tests should prove:
- first clip saves baseline;
- second compatible clip saves initial signal;
- third compatible clip can save validated progress only when gates pass;
- incompatible clip records blockers and does not increment valid count;
- malformed stored precision trend is not trusted by hydration.
  </action>
  <verify>npx vitest run src/actions/history.test.ts src/app/history/analysis-result-hydration.test.ts</verify>
  <acceptance_criteria>
    - `saveAnalysisResult` calls `resolvePrecisionTrend`.
    - Saved `fullResult` contains `precisionTrend`.
    - Active line upsert/update path records baseline/current/checkpoint payload.
    - Incompatible clips are represented as blocked, not silently ignored.
    - Hydration tests cover valid, missing, and malformed precision trend summaries.
  </acceptance_criteria>
  <done>Saving a clip advances or blocks a strict active precision line with an auditable checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Feed strict precision evidence into coach memory</name>
  <files>src/core/coach-memory.ts, src/core/coach-memory.test.ts, src/actions/history.ts, src/actions/history.test.ts</files>
  <read_first>src/core/coach-memory.ts, src/core/coach-plan-builder.ts, src/core/sensitivity-history-convergence.ts, src/core/precision-loop.ts</read_first>
  <action>
Extend coach memory with strict precision trend evidence while keeping Phase 4 coach overhaul deferred.

Required behavior:
- `CoachMemorySnapshot` can include a compact `precisionTrend` or precision signal summary.
- Compatible history used by coach memory should come from the strict compatibility resolver when precision evidence is available.
- Baseline-only and initial-signal states produce validation/recapture signals, not aggressive consolidation.
- Validated progress produces a consolidation signal.
- Validated regression produces a return-to-baseline or validation signal.
- Oscillation and not-comparable produce conservative signals with blocker reasons.
- Sensitivity cannot lead aggressively unless strict trend evidence supports it or evidence remains otherwise strong enough under existing gates.

Do not redesign `CoachPlan` in this phase. Add signals and summaries that Phase 4 can consume, and tests that prove current coach plan tiers can see history conflict/progress/regression reasons.
  </action>
  <verify>npx vitest run src/core/coach-memory.test.ts src/core/coach-plan-builder.test.ts src/actions/history.test.ts</verify>
  <acceptance_criteria>
    - Coach memory includes strict precision compatible count and trend label when available.
    - Baseline/initial signal asks for validation instead of consolidation.
    - Regression/oscillation lowers aggressiveness through history signals.
    - Existing sensitivity conflict downgrade tests still pass.
  </acceptance_criteria>
  <done>Phase 4 receives structured multi-clip evidence without turning Phase 3 into the full coach overhaul.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/db/schema.test.ts
- [ ] npx vitest run src/actions/history.test.ts src/app/history/analysis-result-hydration.test.ts src/core/coach-memory.test.ts src/core/coach-plan-builder.test.ts
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- Active evolution lines persist per strict compatible context.
- Each compatible validation creates a checkpoint.
- Incompatible clips remain auditable with blocker reasons.
- Saved `AnalysisResult.fullResult` includes a structured `precisionTrend` summary.
- Coach memory receives strict multi-clip evidence while existing coach behavior remains conservative.

</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-clip-precision-loop/03-02-SUMMARY.md`
</output>

