---
phase: 02-benchmark-expansion
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/core/captured-golden-promotion.ts
  - src/core/captured-golden-promotion.test.ts
  - src/core/captured-promotion-report.ts
  - src/core/captured-promotion-report.test.ts
  - scripts/promote-captured-clips.ts
  - src/core/promote-captured-clips.test.ts
  - docs/video-benchmark-spec.md
  - docs/benchmark-runner.md
autonomous: true
requirements: [BENCH-02, BENCH-03]
user_setup: []
must_haves:
  truths:
    - "D-09: Real captured clip maturity follows draft -> reviewed -> golden."
    - "D-10: Reviewed can come from human review or machine/Codex-assisted review, but it only counts after structured checks pass."
    - "D-11: Promotion must be easy, safe, and precise through a guided command plus promotion report."
    - "D-12: The guided command validates clip metadata, labels, replay, coverage bucket impact, provenance, and promotion reason."
    - "D-13: The guided command updates JSON artifacts for dataset/labels/provenance and generates a markdown report with checks, coverage impact, reason, and next gaps."
    - "D-14: Missing required metadata blocks promotion and reports missing fields clearly."
    - "D-15: For a reviewed clip to count in the strict gate, expectedTrackingTier is always required; when expected diagnoses exist, expected coach mode and expected coach plan are required."
    - "D-16: Golden requires reviewed status, stable replay, complete metadata, benchmark pass without structural error, strong review, and justification."
    - "D-17: Docs explicitly say benchmark labels and clip promotion are internal maintainer/dev/reviewer processes, not user-facing UGC behavior."
  artifacts:
    - path: "src/core/captured-promotion-report.ts"
      provides: "Promotion report model for checks, coverage impact, reason, blockers, and next gaps."
    - path: "scripts/promote-captured-clips.ts"
      provides: "Guided captured clip promotion command with strict validation and report output."
    - path: "docs/video-benchmark-spec.md"
      provides: "Maintainer/reviewer-only promotion process and maturity rules."
  key_links:
    - from: "scripts/promote-captured-clips.ts"
      to: "src/core/captured-golden-promotion.ts"
      via: "reuse current promotion builder with stricter blockers and report data"
      pattern: "buildCapturedBenchmarkPromotion"
    - from: "src/core/captured-promotion-report.ts"
      to: "src/core/benchmark-coverage.ts"
      via: "promotion report shows coverage impact before and after"
      pattern: "buildBenchmarkCoverageSummary"
    - from: "scripts/promote-captured-clips.ts"
      to: "scripts/run-benchmark.ts"
      via: "promotion validates replay before a clip counts for release"
      pattern: "runBenchmark"
---

<objective>
Upgrade captured clip promotion into a guided maintainer workflow with strict blockers and promotion reports.

Purpose: make real clip promotion safe enough for commercial/release evidence without turning labels into user-facing UGC or silently admitting weak metadata.
Output: stricter promotion validation, markdown promotion report, CLI flags, docs, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-benchmark-expansion/02-CONTEXT.md
@.planning/phases/02-benchmark-expansion/02-RESEARCH.md
@.planning/phases/02-benchmark-expansion/02-01-SUMMARY.md
@src/core/captured-golden-promotion.ts
@scripts/promote-captured-clips.ts
@src/core/captured-specialist-review-kit.ts
@src/core/benchmark-coverage.ts
@tests/fixtures/captured-clips/intake.v1.json
@tests/fixtures/captured-clips/labels.todo.v1.json
@tests/fixtures/captured-clips/review-decisions.todo.v1.json
@docs/video-benchmark-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen promotion validation and blockers</name>
  <files>src/core/captured-golden-promotion.ts, src/core/captured-golden-promotion.test.ts</files>
  <read_first>src/core/captured-golden-promotion.ts, src/types/benchmark.ts, src/types/captured-clip-labels.ts, src/types/captured-benchmark-review-decisions.ts, src/core/captured-golden-promotion.test.ts</read_first>
  <action>
Extend the promotion builder so it reports strict blockers before clips enter reviewed or golden release evidence.

Add blocker categories for:
- missing capture metadata;
- missing `expectedTrackingTier`;
- diagnosed clip missing `expectedCoachMode`;
- diagnosed clip missing expected coach/truth next-block plan;
- missing frame labels path;
- missing or invalid spray window;
- missing provenance/reason for promotion;
- golden requested without reviewed status;
- golden requested without stable replay and successful benchmark structural pass marker;
- golden requested without strong human/specialist/codex-assisted review justification.

Preserve current behavior that approved specialist review wins over older codex-assisted decisions. Preserve current draft -> reviewed -> golden semantics.

Tests must prove:
- reviewed clips require `expectedTrackingTier`;
- diagnosed reviewed clips require coach mode and plan expectations;
- missing metadata blocks promotion with field paths;
- golden cannot be promoted without reviewed status, replay pass, and justification;
- approved specialist provenance still takes precedence.
  </action>
  <verify>npx vitest run src/core/captured-golden-promotion.test.ts</verify>
  <acceptance_criteria>
    - Promotion blockers include clear missing field paths.
    - Reviewed counting prerequisites match D-15.
    - Golden prerequisites match D-16.
    - Existing approved specialist promotion tests still pass.
  </acceptance_criteria>
  <done>Captured promotion refuses incomplete or weakly justified clips before they become release evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add guided CLI report and coverage impact</name>
  <files>src/core/captured-promotion-report.ts, src/core/captured-promotion-report.test.ts, scripts/promote-captured-clips.ts, src/core/promote-captured-clips.test.ts</files>
  <read_first>scripts/promote-captured-clips.ts, src/core/benchmark-coverage.ts, scripts/run-benchmark.ts, src/core/promote-captured-clips.test.ts</read_first>
  <action>
Add a guided promotion report path.

Implementation guidance:
- Add a pure report builder in `src/core/captured-promotion-report.ts`.
- The report should include selected clip IDs, intended maturity, blockers, metadata checks, label checks, replay/benchmark status, coverage impact before/after, provenance, promotion reason, and next gaps.
- Update `scripts/promote-captured-clips.ts` with safe CLI options such as `--dry-run`, `--clip <id>`, `--to reviewed|golden`, `--reason`, `--reviewer`, `--report <path>`, and `--with-review-decisions`.
- Default to dry-run/report-only unless the existing positional command path is being used by current package scripts. Keep current package scripts working.
- When writing dataset/labels/provenance JSON artifacts, also write a markdown report.
- Run captured benchmark or a bounded replay check before claiming a clip is release-counting.

Do not add a browser UI. This is an internal maintainer/dev/reviewer workflow.
  </action>
  <verify>npx vitest run src/core/captured-promotion-report.test.ts src/core/promote-captured-clips.test.ts</verify>
  <acceptance_criteria>
    - Promotion report includes checks, coverage impact, reason, and next gaps.
    - CLI supports a dry-run/report mode.
    - Existing `promote:captured-clips` and `promote:captured-clips:with-review-decisions` scripts still work.
    - Promotion write path generates or returns a markdown report path.
  </acceptance_criteria>
  <done>Maintainers can promote clips through a guided report-backed command instead of opaque JSON rewriting.</done>
</task>

<task type="auto">
  <name>Task 3: Document internal captured benchmark workflow</name>
  <files>docs/video-benchmark-spec.md, docs/benchmark-runner.md, src/core/captured-promotion-report.test.ts</files>
  <read_first>docs/video-benchmark-spec.md, docs/benchmark-runner.md, docs/captured-specialist-review-kit-2026-04-16.md, .planning/phases/02-benchmark-expansion/02-CONTEXT.md</read_first>
  <action>
Update benchmark docs to describe the internal workflow.

Docs must cover:
- benchmark labels and clip promotion are internal maintainer/dev/reviewer processes, not public UGC;
- maturity path: draft -> reviewed -> golden;
- reviewed counting rules for strict release gate;
- golden prerequisites;
- required metadata: patch, weapon, optic, attachments, distance, spray window, labels, provenance;
- how to run dry-run promotion, report generation, strict release gate, and baseline update when behavior intentionally changes;
- how coverage impact should be interpreted without claiming perfect sensitivity or guaranteed improvement.

Add or update tests so docs retain internal-only language and do not imply public user labeling.
  </action>
  <verify>npx vitest run src/core/captured-promotion-report.test.ts src/ci/benchmark-workflow.test.ts</verify>
  <acceptance_criteria>
    - Docs contain `draft -> reviewed -> golden`.
    - Docs explicitly say promotion is internal maintainer/dev/reviewer workflow.
    - Docs explain missing metadata blockers.
    - Docs tell maintainers to run `benchmark:release` before commercial claims.
  </acceptance_criteria>
  <done>Captured clip promotion is documented as a safe internal release-evidence workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/core/captured-golden-promotion.test.ts src/core/captured-promotion-report.test.ts src/core/promote-captured-clips.test.ts
- [ ] npm run benchmark:release
- [ ] npm run typecheck
- [ ] npx vitest run
- [ ] npm run benchmark:gate
</verification>

<success_criteria>

- Captured clips cannot count for strict release evidence without required metadata, labels, provenance, replay/benchmark validity, and promotion reason.
- Reviewed and golden maturity are distinct and reported separately.
- Promotion writes or previews JSON changes with a markdown report.
- Docs make the workflow internal-only and avoid commercial overclaiming.

</success_criteria>

<output>
After completion, create `.planning/phases/02-benchmark-expansion/02-03-SUMMARY.md`
</output>

