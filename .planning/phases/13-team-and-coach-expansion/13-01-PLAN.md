---
phase: 13-team-and-coach-expansion
plan: "01"
type: execute
wave: 1
depends_on:
  - "13-00"
files_modified:
  - src/types/team-coach.ts
  - src/types/team-coach.test.ts
  - src/types/monetization.ts
  - src/types/monetization.test.ts
  - src/lib/product-entitlements.ts
  - src/lib/product-entitlements.test.ts
  - src/lib/team-coach-access.ts
  - src/lib/team-coach-access.test.ts
  - src/lib/premium-projection.ts
  - src/lib/premium-projection.test.ts
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
must_haves:
  truths:
    - "Mesa do Coach is a separate Team capability domain; solo Pro and Social Pro do not grant coach workspace/player-review authority."
    - "`team.player_review` gates workspace creation/review/export workflows and `team.seats` gates seat/accounting foundation."
    - "All Team denials are server-owned and reasoned: no workspace membership, role blocked, invite expired, consent missing, report revoked, Team entitlement missing, source not shared, and seat limit reached."
    - "Decision coverage anchors: D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08 D-09 D-10 D-32 D-33 D-34 D-35 D-47 D-48 D-49 D-50 D-51 D-52 D-53 D-54 D-55 D-56 D-57 D-58 D-59 D-60 D-61 D-62 D-63."
  artifacts:
    - path: "src/types/team-coach.ts"
      provides: "Team/Coach workspace, role, consent, share, review, packet, seat, denial, and audit contracts"
    - path: "src/lib/team-coach-access.ts"
      provides: "Server-owned Team capability policy"
    - path: "src/lib/premium-projection.ts"
      provides: "Team lock/projection copy and capability booleans"
  key_links:
    - from: "src/lib/team-coach-access.ts"
      to: "src/lib/product-entitlements.ts"
      via: "team.player_review and team.seats entitlement checks"
      pattern: "team.player_review|team.seats"
    - from: "src/lib/premium-projection.ts"
      to: "src/app/mesa-coach/page.tsx"
      via: "Team locked-state projection"
      pattern: "canUseTeam|team"
---

<objective>
Define the Team/Coach domain contracts and server-owned access resolver.

Purpose: every later plan depends on stable roles, consent scopes, review statuses, denial reasons, seat states, and a Team access policy that is separate from solo Pro and Social Pro.
Output: Team/Coach type contracts, Team entitlement grouping/access policy, premium projection Team locks, and focused tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-team-and-coach-expansion/13-CONTEXT.md
@.planning/phases/13-team-and-coach-expansion/13-RESEARCH.md
@src/types/monetization.ts
@src/lib/product-entitlements.ts
@src/lib/social-pro-access.ts
@src/lib/premium-projection.ts
@src/lib/product-access-server.ts

<interfaces>
Existing Social Pro policy pattern:
```typescript
export function createSocialProAccessPolicy(input: {
  readonly productAccess: ProductAccessResolution;
  readonly userRole?: SocialProUserRole;
}): SocialProAccessPolicy;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Team/Coach contracts</name>
  <files>src/types/team-coach.ts, src/types/team-coach.test.ts</files>
  <behavior>
    - Export zod-backed enum contracts with `values`, `schema`, `isValue`, and `parse` helpers for workspace role, workspace status, membership status, invite status, consent scope/status, share status, review status, packet visibility/status, private link status, seat state, denial reason, audit event type, and next-action kind.
    - Contracts include `owner`, `coach`, `analyst`, and `player` roles; `viewer` is allowed only if tests keep permissions simple.
    - Review statuses include `needs_review`, `reviewed`, `waiting_player`, `validation_requested`, `repair_requested`, and `archived`.
    - Denial reasons include no workspace membership, role blocked, invite expired, consent missing, report revoked, Team entitlement missing, source not shared, and seat limit reached.
  </behavior>
  <action>Create `src/types/team-coach.ts` with narrow contracts for Phase 13. Keep names stable and pt-BR display copy out of core contracts unless tests require label helpers.</action>
  <verify>
    <automated>npx vitest run src/types/team-coach.test.ts</automated>
  </verify>
  <done>Team/Coach contracts compile and tests cover valid/invalid enum parsing and required role/status/reason values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Team entitlement grouping and access policy</name>
  <files>src/types/monetization.ts, src/types/monetization.test.ts, src/lib/product-entitlements.ts, src/lib/product-entitlements.test.ts, src/lib/team-coach-access.ts, src/lib/team-coach-access.test.ts</files>
  <behavior>
    - Team keys remain distinct from solo Pro and Social Pro capability grants.
    - `createTeamCoachAccessPolicy` maps product access plus workspace role/membership/consent context to explicit capabilities.
    - Capabilities cover public read preview, create workspace, manage workspace, invite, accept invite, review roster, open player dossier, create/revoke share, write coach note, update review status, create/edit Team Review Packet, manage packet links, manage seats, and moderate unsafe packets if admin.
    - Tests prove anonymous/free/solo Pro/Social Pro/canceled users cannot create or review Team artifacts without Team entitlement and membership.
    - Manual/admin Team beta grants are supported through existing server-owned access truth without activating self-serve seat billing.
  </behavior>
  <action>Implement the Team access resolver analogously to Social Pro, but include workspace role and consent context. Do not add Team keys to default solo Pro grants unless a test explicitly proves the access remains separately gated.</action>
  <verify>
    <automated>npx vitest run src/types/monetization.test.ts src/lib/product-entitlements.test.ts src/lib/team-coach-access.test.ts</automated>
  </verify>
  <done>Team access is server-owned, separate from solo Pro/Social Pro, and returns stable capability booleans plus denial reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose Team lock/projection copy</name>
  <files>src/lib/premium-projection.ts, src/lib/premium-projection.test.ts</files>
  <behavior>
    - Projection summary exposes Team capability booleans for player review and seats.
    - Locks preview Mesa do Coach value as workflow value: roster review, privacy-safe packets, next-action coordination, and future seat foundation.
    - Copy does not imply solo Pro grants Team access, real seat billing is live, or Team output is certification/rank proof.
  </behavior>
  <action>Extend premium projection for Team locks and capabilities using existing copy style. Keep Free/public community basics visible and do not close public community behavior.</action>
  <verify>
    <automated>npx vitest run src/lib/premium-projection.test.ts src/app/copy-claims.contract.test.ts</automated>
  </verify>
  <done>Team lock/projection tests prove Team value is visible without granting access through client state or overclaiming authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| product access -> Team capability | Product access truth is converted into Team-specific authority. |
| workspace role/consent -> action permission | Membership and consent must constrain every Team action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13-01-01 | Elevation of Privilege | `src/lib/team-coach-access.ts` | mitigate | Test that solo Pro/Social Pro/client state cannot grant Team capabilities. |
| T-13-01-02 | Spoofing | Team entitlement | mitigate | Use server-owned product access and stable denial reasons. |
| T-13-01-03 | Information Disclosure | projection copy | mitigate | Projection shows locks/value, not private player data or raw Team state. |
</threat_model>

<verification>
Focused checks:
- `npx vitest run src/types/team-coach.test.ts src/types/monetization.test.ts src/lib/product-entitlements.test.ts src/lib/team-coach-access.test.ts src/lib/premium-projection.test.ts`
- `npm run test:monetization`
- `npm run typecheck`
</verification>

<success_criteria>
Team/Coach contracts and access policies are stable, server-owned, separately gated from solo Pro/Social Pro, and ready for persistence/actions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-team-and-coach-expansion/13-01-SUMMARY.md`.
</output>
